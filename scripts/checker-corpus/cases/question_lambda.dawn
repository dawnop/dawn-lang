# checker.dawn: `?` inside a lambda, and outside any function that can carry it
pub fn in_lambda(x: Option[Int]) -> Option[Int] = {
  let f = () => x?
  x
}

test "question mark in a test block" {
  let x: Option[Int] = Some(1)
  let y = x?
  assert y == 1
}
