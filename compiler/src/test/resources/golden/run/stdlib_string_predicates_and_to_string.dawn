use std/str

pub fn main() -> Unit !io = {
  println("${str.contains("hello", "ell")} ${str.starts_with("hello", "he")} ${str.ends_with("hello", "lo")}")
  println(to_string(42) ++ to_string(true) ++ to_string(1.5))
}
