The whole program is a taken barrier: the panic is caught by the bracket's
handler, the release runs, and the panic resumes past a `catch_fault` that
declines it. A taken catch jumps past C frames whose references nothing can
release, so leak detection is off for it; see the header of run.sh.
