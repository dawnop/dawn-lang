# A consumer of packages/tea, the same relationship the json example has to
# packages/json: the demo exercises the package through [deps], it is not a
# second copy of it.
schema = 1
name = "tea_todo"
version = "1.0.0"

[deps]
tea = "../../../packages/tea"
