D	trait_method_effects.dawn	1129	1130	`m` is declared !Ask but trait `Plain` declares it pure	drop !Ask here, or declare the trait method !Ask; if impls of `Plain` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!T.E`
D	trait_method_effects.dawn	1160	1161	`n` binds 1 effect variable(s) but trait `Plain` binds 0	they are paired in the order they are written; trait `Plain` declares it as fn n(Sub) -> Int, this impl declares fn n(Sub) -> Int !e
D	trait_method_effects.dawn	1769	1771	`sq` does not match the trait's signature	trait `Sigs` declares it as fn sq(Sub) -> Int
D	trait_method_effects.dawn	1817	1819	`sr` does not match the trait's signature	trait `Sigs` declares it as fn sr(Sub) -> Int
D	trait_method_effects.dawn	2288	2290	`tw` does not match the trait's signature	trait `Two` declares it as fn tw(Sub, fn() -> Int !b, fn() -> Int !a) -> Int !b
