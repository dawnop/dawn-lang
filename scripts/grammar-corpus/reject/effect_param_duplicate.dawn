# expect: duplicate effect parameter `e`
# One name, one variable: the resolver mints a name once and the binder list is
# where it mints from, so a repeated binder would name one variable twice and
# put it in `Sig.eparams` twice.
fn f[!e, !e](x: Int) -> Int = x
