# expect: `io` is a built-in effect and cannot be a parameter name
# `io` is one of the two base points, not a variable: a signature that bound it
# would have two spellings for the same row and no way to tell them apart.
fn f[!io](x: Int) -> Int = x
