# checker.dawn: type parameters that no use site pins down
use std/list

fn ident[T](x: T) -> T = x

fn pair[A, B](a: A, b: B) -> Int = 1

pub fn fn_as_value() -> Int = {
  let f = ident
  1
}

pub fn ctor_as_value() -> Int = {
  let g: fn(Int) -> Int = Some
  1
}

pub fn ctor_unpinned() -> Int = {
  let h = None
  1
}
