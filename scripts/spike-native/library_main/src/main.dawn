# The entry module of a project whose library also defines a `main`: only
# this one is the program's entry point, on both backends.
use util

pub fn main() -> Unit !io = {
  println(to_string(util.doubled(21)))
  println(to_string(util.main_again()))
}
