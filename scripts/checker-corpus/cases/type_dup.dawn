# checker.dawn: a type name is declared once; prelude types are not redefinable
type Thing = A | B

type Thing = C

type Option[T] = Nope

type Result[T, E] = Bad

type ForeignError = Boom

type Twins[T, T] = One(x: T)
