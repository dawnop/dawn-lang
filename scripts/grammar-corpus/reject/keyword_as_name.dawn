# expect: alias
# `alias` is a keyword (spec 2.6), so it cannot be a binding name -- the error
# names it rather than complaining about the `=` that follows.
fn f() -> Int = {
  let alias = 1
  alias
}
