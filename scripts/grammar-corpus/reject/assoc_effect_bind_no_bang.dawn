# expect: `!` (the binding is a row: !SomeEffect, !io, or the empty row !())
# expect: only declarations (use, fn, type, alias, const, trait, impl, effect, test) are allowed
trait P[T] {
  effect E
  fn run(x: T) -> Int
}
type A = { v: Int }
impl P[A] {
  effect E = Ask
  fn run(x: A) -> Int = x.v
}
