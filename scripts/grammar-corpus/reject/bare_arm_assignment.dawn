# expect: match arm
# A match arm's body is an expression; an assignment is a statement, so it
# needs braces. Getting this wrong is the repository's most-repeated syntax
# slip, which is why it is pinned here.
fn f(n: Int) -> Int = {
  var x = 0
  match n {
    0 -> x = 1
    _ -> x = 2
  }
  x
}
