This program takes fault barriers on purpose (that is what it tests). A taken
catch longjmps past C frames whose references nothing can release, so leak
detection is off for it -- see the header of run.sh.
