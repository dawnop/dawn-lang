// Vite's ambient types, for the `?raw` imports in samples.ts. Nothing in this
// package runs `tsc` -- vite and esbuild transpile without checking -- so this
// is for the editor, not for the build.
/// <reference types="vite/client" />
