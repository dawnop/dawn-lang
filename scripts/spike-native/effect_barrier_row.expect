-- a label read inside the protected closure --
301
-- and read on a path whose value the failure discards --
panicked after 42
-- a handler arm performing an operation under the barrier --
510
-- a second barrier inside that arm, taking the failure --
609
-- catch_panic, the other barrier and the same signature --
706
-- the row named as a variable and instantiated at the call --
908
-- a projection in the row, which is neither label nor variable --
1009
-- a label beside a variable, the residual bound by #335 --
1107
-- a barrier taken as a value rather than called --
1210
-- the control: closures with nothing to read --
1300
1400
