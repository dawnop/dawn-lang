# `T.Item`'s trait is private, so a caller cannot write the projection down
# even though the subject is the function's own type parameter.
trait Hidden[T] {
  type Item
  fn item(x: T) -> T.Item
}

pub fn project[T: Hidden](x: T) -> T.Item = item(x)
