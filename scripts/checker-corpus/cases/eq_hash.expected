D	eq_hash.dawn	187	189	functions cannot be compared	compare the values they produce, not the functions
D	eq_hash.dawn	238	240	`Unit` has no equality	there is one Unit and it carries no information, so the answer would be a constant
D	eq_hash.dawn	288	294	`Float` has no hash	NaN and -0.0 give Float two different equalities, so no hash can agree with `==`; use an Int encoding (scaled integer, bits) or a String rendering instead
D	eq_hash.dawn	369	375	functions cannot be compared, and `Boxed` contains one	compare the values they produce, not the functions
D	eq_hash.dawn	369	375	functions cannot be hashed, and `Boxed` contains one	hash the values they produce, not the functions
