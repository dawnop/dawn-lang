# An erased Object is not a Path. The caller must first use checked `cast`.

use java "java.lang.Object"
use java "java.nio.file.Files"

pub fn implicit_narrow(value: Object) -> Bool !io = Files.exists(value)
