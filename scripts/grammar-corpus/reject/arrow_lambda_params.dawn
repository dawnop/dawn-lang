# expect: invalid parameter list before `=>`
# The cover-grammar failure: `(` is reinterpreted as a parameter list when the
# `=>` arrives, and `a + 1` is not one. The message names the offending token
# rather than reporting a generic parse error (spec §4.5).
fn f() -> Unit = (a + 1) => a
