schema = 1
name = "builtin_decl_dump"

# The compiler's own source, as a path dependency. The builtin table is a Dawn
# value in `check/types.dawn`, so reading it needs no compiler API and no new
# subcommand: this project imports the module and prints what it holds.
[deps]
selfhost = "../../../selfhost"
