D	entry.dawn	194	198	`seam` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	200	205	`Trace` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	343	348	`Trace` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	385	394	`Seam` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	268	272	`seam` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	285	294	`Seam` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	331	336	`LIMIT` is package-private to package `lib`	only modules of package `lib` may name it
D	entry.dawn	426	430	`Seam` is package-private to package `lib`	only modules of package `lib` may name it
