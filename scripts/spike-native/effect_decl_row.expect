-- an alias binder in parameter position, no variable beside it --
2
an io thunk
3
-- and the same alias beside a consumer variable that is read --
105
-- a record field's binder consumed the same way --
207
-- with the continuation as a written value rather than a lambda --
311
-- a variant field's binder, both arms --
413
513
-- two consumer variables under one io --
1236
-- the row that says only !io, with the variable in a parameter --
1131
-- the io happening before the call, which is the bug's ordering --
io_then ran
723
-- and the same inferred row inside a primitive's use closure --
inside the use closure
829
-- the control: continuations with nothing to read --
901
io_then ran
1001
