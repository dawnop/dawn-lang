# expect: expected an uppercase name after `as` (a rename keeps the case of `Fs`), found `files`
# Case is meaning, so a rename keeps the token kind of the name it renames: an
# effect cannot be given a variable's spelling.
use std/io.{Fs as files}
