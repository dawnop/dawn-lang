effect Ask {
  fn ask() -> Int
}

# Pure. The signature says it asks; it does not say whom
fn total() -> Int !Ask = ask() + ask()

pub fn main() -> Unit !io = {
  with handle Ask { ask() => 21 }
  println("${total()}")
}
