# expect: an effect operation cannot have a body
# The body of an operation is the handler arm, and a handler is installed at a
# use site rather than declared once with the effect.
effect Ask {
  fn ask() -> Int = 1
}
