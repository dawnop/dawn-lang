D	trait_method_effects.dawn	373	374	trait methods cannot declare the effect `Ask`	a trait method is pure or !io
D	trait_method_effects.dawn	432	448	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	trait_method_effects.dawn	942	943	impl methods cannot declare the effect `Ask`	an impl method is pure or !io
D	trait_method_effects.dawn	973	974	`n` binds 1 effect variable(s) but trait `Plain` binds 0	they are paired in the order they are written; trait `Plain` declares it as fn n(Sub) -> Int, this impl declares fn n(Sub) -> Int !e
D	trait_method_effects.dawn	924	929	impl `Plain[Sub]` is missing `m`	provide: fn m(x: Sub) -> Int
D	trait_method_effects.dawn	1526	1542	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	trait_method_effects.dawn	1512	1514	`sq` does not match the trait's signature	trait `Sigs` declares it as fn sq(Sub) -> Int
D	trait_method_effects.dawn	1560	1562	`sr` does not match the trait's signature	trait `Sigs` declares it as fn sr(Sub) -> Int
D	trait_method_effects.dawn	2031	2033	`tw` does not match the trait's signature	trait `Two` declares it as fn tw(Sub, fn() -> Int !b, fn() -> Int !a) -> Int !b
