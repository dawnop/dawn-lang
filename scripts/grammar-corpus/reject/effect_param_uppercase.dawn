# expect: `Ask` is a declared effect, not an effect parameter
# A declaration binds effect variables, never labels: a label belongs to the
# signature that raises it (effects-soundness-design.md §4.1). The casing rule
# is the same one cx.resolve_eff_at applies inside a row.
fn f[!Ask](x: Int) -> Int = x
