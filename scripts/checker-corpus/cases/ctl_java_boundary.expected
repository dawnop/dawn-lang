D	ctl_java_boundary.dawn	1521	1534	a function value whose row names the `ctl` effect `Async` cannot cross to Java	`ctl` says an arm may suspend this value's caller, and there is no Dawn frame under a Java frame to suspend into; move the conversion out, or drop `ctl` from the effect if no arm of it binds a continuation
D	ctl_java_boundary.dawn	1882	1913	a function value whose row names the `ctl` effect `Async` cannot cross to Java	`ctl` says an arm may suspend this value's caller, and there is no Dawn frame under a Java frame to suspend into; move the conversion out, or drop `ctl` from the effect if no arm of it binds a continuation
D	ctl_java_boundary.dawn	2140	2153	a function value whose row names the `ctl` effect `Async` cannot cross to Java	`ctl` says an arm may suspend this value's caller, and there is no Dawn frame under a Java frame to suspend into; move the conversion out, or drop `ctl` from the effect if no arm of it binds a continuation
