# A function that calls itself cannot have its return type inferred. `g`
# only calls it: it is checked once `f` is sealed as an error and is not
# reported as recursive.
fn f(n: Int) = if n == 0 { 0 } else { f(n - 1) }
fn g() = f(3) + 1
