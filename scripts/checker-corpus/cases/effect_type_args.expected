D	effect_type_args.dawn	1740	1742	`!e` cannot be an effect argument to `Thunk`: a type declaration's effect parameter carries no evidence (spec §6.3)	a variable bound elsewhere owes evidence that its binder's frame holds; write `!io`, which covers any row, or omit the argument and let the declaration's own variable stand
D	effect_type_args.dawn	692	701	`Pair` binds no effect parameters	an effect argument answers a `[!e]` in the declaration's parameter list, and this one has none
D	effect_type_args.dawn	793	802	`List` binds no effect parameters	an effect argument answers a `[!e]` in the declaration's parameter list, and this one has none
D	effect_type_args.dawn	1018	1027	`Cell` takes 0 type parameter(s), got 1	`Cell` also binds the effect parameter(s) `!e`, written with the `!`: `Cell[!io]`
D	effect_type_args.dawn	1116	1131	`Thunk` binds 1 effect parameter(s), got 2	
D	effect_type_args.dawn	1352	1356	`!Ask` cannot be an effect argument to `Thunk`: a type declaration's effect parameter carries no evidence (spec §6.3)	a handler is supplied where the closure is called, so a written label needs a frame to arrive at; bind the variable on the function that runs the closure, or write `!io`
D	effect_type_args.dawn	1524	1526	`!e` cannot be an effect argument to `Thunk`: a type declaration's effect parameter carries no evidence (spec §6.3)	a variable bound elsewhere owes evidence that its binder's frame holds; write `!io`, which covers any row, or omit the argument and let the declaration's own variable stand
D	effect_type_args.dawn	1906	1915	`Cell` is a record type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Cell` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	effect_type_args.dawn	2001	2011	`Chain` is a variant type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Chain` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	effect_type_args.dawn	2227	2238	`Hidden` is an opaque type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Hidden` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	effect_type_args.dawn	2461	2462	function `io_slot` is not declared !io but calls `t` (!io)	add !io to the end of the signature, or remove the call
D	effect_type_args.dawn	2737	2738	function `own_var` is not declared !e but calls `m` (!e)	`!e` is bound where it was written, so writing `!e` on the end of the signature would bind a different variable: declare !io, which covers any row, or remove the call
