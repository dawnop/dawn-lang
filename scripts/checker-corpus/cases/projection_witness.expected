D	projection_witness.dawn	451	455	no `Show` for the projection `C.Item`	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
D	projection_witness.dawn	523	525	no `Eq` for the projection `C.Item`	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
D	projection_witness.dawn	595	596	no `Ord` for the projection `C.Item`	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
D	projection_witness.dawn	673	684	no `Eq` for the projection `C.Item`	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
D	projection_witness.dawn	673	684	no `Hash` for the projection `C.Item`	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
D	projection_witness.dawn	835	837	`C.Item` has no equality, and `(C.Item, Int)` contains one	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
D	projection_witness.dawn	983	990	no `Index` for the projection `C.Item`	a bound applies to a type parameter, and `C.Item` is not one -- it is read off whichever impl the caller picks; return the value and do this at the call site, where its type is concrete
