# checker.dawn: trait declaration collisions
type Clash = X

trait Clash[T] { fn a(x: T) -> Int }

trait Eq[T] { fn b(x: T) -> Int }

trait Mine[T] { fn c(x: T) -> Int }

trait Mine[T] { fn d(x: T) -> Int }

trait Dup[T] {
  type It
  type It
  fn e(x: T) -> Int
  fn e(x: T) -> Int
}
