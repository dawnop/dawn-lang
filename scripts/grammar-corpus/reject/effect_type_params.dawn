# expect: an effect cannot declare type parameters
# expect: expected `=` (function body)
# v1 effects are bare labels (D2): a parameterised effect needs effect
# parameters in the type-parameter table, which is RX-10-B's separate route.
effect Yield[T] {
  fn emit(x: T) -> Unit
}
