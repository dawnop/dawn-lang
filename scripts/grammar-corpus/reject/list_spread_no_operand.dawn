# expect: `..` needs a list to spread
# A spread at an element position with nothing after it. Caught in `list_elem`
# rather than left to the expression parser, which would name whatever closing
# token happened to follow instead of the `..` that is actually wrong.
fn f() -> List[Int] = [..]
