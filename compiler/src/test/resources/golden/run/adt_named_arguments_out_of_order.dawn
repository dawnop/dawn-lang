type Shape =
  | Circle(r: Float)
  | Rect(w: Float, h: Float)
  | Point

pub fn main() -> Unit !io = {
  let r = Rect(h: 3.0, w: 2.0)
  match r {
    Rect(w, h) -> println("w=$w h=$h")
    _ -> println("no")
  }
}
