# Nothing calls it, nothing imports it: the exporter still has to refuse it,
# because a public contract is wrong where it is written and not where it is
# eventually used (design §7.1).
type Secret = Secret

pub fn never_used() -> Secret = Secret

pub fn used() -> Int = helper()

fn helper() -> Int = 0
