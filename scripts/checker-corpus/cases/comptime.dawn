# checker.dawn: what a comptime block may produce and use
effect Ask { fn get() -> Int }

pub fn handled() -> Int = comptime {
  with handle Ask { get() => 1 }
  2
}

pub fn as_fn() -> fn() -> Int = comptime { () => 1 }
