schema = 1
# v2 of the package the repo spells `json`: a major bump renames the package
# (the v2-in-name rule), and consumers keep their `use json/...` lines by
# aliasing: `json = "../json"` here, `[deps.json]` at a url downstream.
name = "json2"
version = "2.0.0"
