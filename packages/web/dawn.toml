schema = 1
# v3 of the package the repo spells `web`: a major bump renames the package
# (the v2-in-name rule), and consumers keep their `use web/...` lines by
# aliasing — `web = { path = ... }` here, `[deps.web]` at a url downstream.
name = "web3"
version = "3.0.0"

[deps]
json = "../json"
