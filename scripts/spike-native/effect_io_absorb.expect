-- a variable absorbed by the io written beside it --
under_io ran
300
-- the row that says only !io --
300
-- the control: a row with no io to absorb anything --
300
-- two absorbed variables, two labels, one charge --
307
-- forwarded through a second absorbing signature --
7
-- and a pure closure through the same signatures --
1
