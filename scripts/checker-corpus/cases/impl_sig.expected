D	impl_sig.dawn	152	153	`a` does not match the trait's signature	trait `One` declares it as fn a(Sub) -> Int
D	impl_sig.dawn	262	263	`p` is declared !io but trait `Pure` declares it pure	drop !io here, or declare the trait method !io; if impls of `Pure` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!T.E`
