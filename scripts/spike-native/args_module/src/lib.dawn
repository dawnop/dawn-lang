# `args` is process input, not state owned by the module that reads it. Keeping
# the read outside the entry module catches a JVM emitter that points it at the
# caller class's uninitialised argv field.
use std/io

pub fn first_arg() -> String !io = match args() {
  [] -> "<none>"
  [a, ..] -> a
}
