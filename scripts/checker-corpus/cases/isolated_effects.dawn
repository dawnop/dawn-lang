# checker.dawn: leaving a comptime block puts the enclosing effect record and
# its witness back (Frame.used_effects, Frame.eff_witness) -- the io reported
# below is the caller's `println`, at the caller's span, not the block's
pub fn calls_io() -> Int = {
  println("outer io")
  let n = comptime { 2 }
  n
}
