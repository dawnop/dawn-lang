This program takes a barrier on purpose: every line runs a failing subscript
inside `catch_panic` so the message can be printed. On native a taken barrier
leaks what the discarded C frames held -- longjmp cannot release them, and
that is the documented cost of the mechanism, not a hole in the reference
counting. See the header of run.sh.
