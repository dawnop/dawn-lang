# checker.dawn: an impl method must match the trait's signature and effect row
trait One[T] {
  fn a(x: T) -> Int
}

type Sub = S

impl One[Sub] {
  fn a(x: Sub, y: Int) -> Int = 1
}

trait Pure[T] {
  fn p(x: T) -> Int
}

type Sub2 = S2

impl Pure[Sub2] {
  fn p(x: Sub2) -> Int !io = 1
}
