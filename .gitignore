.gradle/
build/
*.jar
# ...except the wrapper jar, which pins the Gradle version and must travel with
# the repo — without it `./gradlew` cannot bootstrap on a fresh checkout.
!gradle/wrapper/gradle-wrapper.jar
*.class
.idea/
.DS_Store
node_modules/
*.vsix

# M5 site
site/dist/
site/build/

# play-ui (built locally, vendored into site/assets)
site/play-ui/node_modules/
site/play-ui/dist/
site/play-ui/src/builtins.generated.ts
site/build/
