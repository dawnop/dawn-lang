D	entry.dawn	1128	1141	`Cell` is a record type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Cell` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	entry.dawn	922	923	function `io_slot` is not declared !io but calls `t` (!io)	add !io to the end of the signature, or remove the call
D	entry.dawn	1109	1110	function `elided_pure` is not declared !e but calls `t` (!e)	`!e` is `Thunk`'s own effect parameter, so writing `!e` on the end of the signature would bind a different variable: write `Thunk`'s effect argument at the type application, spelling a row this signature does bind, or declare !io, which covers any row
