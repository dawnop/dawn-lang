# expect: `with` is a statement
# `with` swallows the rest of its block, so it needs a block to be in. A
# body written without braces has no "rest", and the diagnostic says so rather
# than reporting an unexpected token.
fn f(path: String) -> Unit !io = with h <- bracket(open(path), close)
