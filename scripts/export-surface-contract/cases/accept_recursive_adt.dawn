# A public ADT that mentions itself. The nominal identity is checked at every
# reference and the fields exactly once, where the type is its own root -- a
# walker that recursed into fields at each reference would not terminate here.
pub type Tree = Leaf | Node(left: Tree, right: Tree, kids: List[Tree])

pub fn leaf() -> Tree = Leaf
