D	local_fn.dawn	77	82	local functions cannot declare effect variables	a local function is `!io` or pure; lift it to the top level for effect polymorphism
D	local_fn.dawn	177	180	the body of `inner` has type String but the declared return type is Int	
D	local_fn.dawn	221	226	the body of `inner` performs io	declare it: fn inner(...) -> Unit !io = ...
D	local_fn.dawn	299	304	the body of `local` performs io	declare it: fn local(...) -> (fn() -> Int) !io = ...
D	local_fn.dawn	740	743	`inner` uses the effect `Ask`, but a local function cannot carry an effect label	lift `inner` to the top level and declare !Ask, or handle it inside its body with `with handle Ask { ... }`
D	local_fn.dawn	1017	1020	`inner` uses the effect `Ask`, but a local function cannot carry an effect label	lift `inner` to the top level and declare !Ask, or handle it inside its body with `with handle Ask { ... }`
D	local_fn.dawn	2123	2128	`probe` uses the effect variable `!e`, but a local function cannot carry an effect variable	lift `probe` to the top level so its signature can declare !e
D	local_fn.dawn	2522	2525	`probe` uses the effect projection `!T.E`, but a local function cannot carry an effect projection	lift `probe` to the top level so its signature can declare !T.E
