# A spread contributes its operand's *item* type to the element type. Refused
# because a List[String] cannot be spliced in among Ints.
fn f(xs: List[String]) -> Int = len([1, ..xs])

pub fn main() -> Unit = ()
