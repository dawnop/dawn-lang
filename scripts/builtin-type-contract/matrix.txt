# The persistent mutant matrix for builtin-type-contract, one name per line
# in run order. run.sh validates its executable list against this file in both
# directions at startup, and scripts/mutant-coverage/check.py holds the union
# of the CI shards to it. Round-robin sharding reads positions off this order,
# so reordering lines moves mutants between shards (harmless, but say so in
# the commit if you do it on purpose).
stale-checker-consumer
stale-lsp-consumer
omit-return-lsp
omit-return-hover
omit-doc-type
flatten-return-doc
omit-public-function-doc
omit-prelude-function-doc
reject-top-return
reject-local-return
reject-trait-return
reject-impl-return
reject-effect-return
reject-function-type-return
allow-storage-parameter
allow-storage-field
allow-storage-const
allow-storage-let
allow-storage-generic
allow-storage-tuple
allow-function-parameter
allow-storage-assoc
allow-direct-alias
allow-reserved-name
allow-never-fallthrough
make-io-exit-bottom
