# checker.dawn: lambda and `with`-closure arity
pub fn takes(f: fn(Int, Int) -> Int) -> Int = f(1, 2)

pub fn wrong_arity() -> Int = takes(x => x)

fn bracketish(a: Int, k: fn(Int, Int) -> Int) -> Int = k(a, a)

pub fn with_arity() -> Int = {
  with x <- bracketish(1)
  x
}
