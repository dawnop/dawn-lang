# A call with no written arguments, where inserting the left side and appending
# it are the same edit. Only wrapping the call differs here, which is what
# makes this the narrowest witness for "the pipe changes a call's arguments".
fn twice(n: Int) -> Int = n * 2

pub fn main() -> Unit !io = println("${21 |> twice()}")
