# The persistent mutant matrix for builtin-decl-contract, one name per line in
# run order. run.sh validates its executable list against this file in both
# directions at startup.
#
# This harness is not sharded: a mutant here is a string edit to two files held
# in memory, so the whole set runs in well under a second inside one job step.
# It therefore does not source scripts/mutant-coverage/shard.sh, and
# scripts/mutant-coverage/check.py does not expect a coverage report from it.
p1-declare-a-name-the-compiler-has-not
p2-drop-popcount
p3-rename-a-parameter
p3-widen-a-return-type
p4-publish-str_lower
p4-hide-parse_int_radix
p5-move-a-comptime-marker
p6-drop-sort_by-s-effect-binder
p6-skip-a-signature-that-reads-back
