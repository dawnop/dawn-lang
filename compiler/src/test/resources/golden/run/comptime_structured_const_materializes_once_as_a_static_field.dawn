const TABLE: List[Int] = comptime {
  range(0, 5) |> map(fn(x) => x * x)
}

pub fn main() -> Unit !io = {
  match TABLE.get(4) {
    Some(v) -> println("$v")
    None -> println("?")
  }
  println("${len(TABLE)}")
}
