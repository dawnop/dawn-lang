trait Hidden[T] {
  fn hidden(x: T) -> Bool
}

pub type Holder[T] = { value: T }

pub trait HasItem[T] {
  type Item
  fn item(x: T) -> T.Item
}

impl[T: Hidden] HasItem[Holder[T]] {
  type Item = T
  fn item(x: Holder[T]) -> T = x.value
}
