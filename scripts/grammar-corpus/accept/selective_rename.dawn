# `use m.{Name as Local}` renames one entry of a selective import, for any kind
# of member (spec §10.2); a whole-module alias keeps its own `as`.
use std/io.{Fs as Files, Env}
use std/list.{map as list_map}
use std/io as sysio

pub fn main() -> Unit !io = ()
