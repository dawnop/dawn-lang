module std/list
fn std/list.map_go -> List[U]
  param v0 : List[T] owned
  param v1 : fn(T) -> U !e owned
  param v2 : Int owned
  param v3 : List[U] owned
  block : List[U]
    loop L0
      return
        if : List[U]
          binary eq : Bool
            local v2 : Int
            intrinsic len : Int
              local v0 : List[T]
          block : List[U]
            local v3 : List[U]
          block : List[U]
            block : Never
              let v4 : List[T]
                local v0 : List[T]
              let v5 : fn(T) -> U !e
                local v1 : fn(T) -> U !e
              let v6 : Int
                binary add : Int
                  local v2 : Int
                  int 1
              let v7 : List[U]
                binary concat : List[U]
                  local v3 : List[U]
                  listlit : List[U]
                    call dynamic : U
                      local v1 : fn(T) -> U !e
                      intrinsic expect : T
                        intrinsic get : Option[T]
                          local v0 : List[T]
                          local v2 : Int
                        str "index below length"
              assign v0
                local v4 : List[T]
              assign v1
                local v5 : fn(T) -> U !e
              assign v2
                local v6 : Int
              assign v3
                local v7 : List[U]
              continue L0
fn std/list.map -> List[U]
  param v0 : List[T] owned
  param v1 : fn(T) -> U !e owned
  block : List[U]
    let v2 : List[U]
      listlit : List[U]
    call direct std/list.map_go : List[U]
      local v0 : List[T]
      local v1 : fn(T) -> U !e
      int 0
      local v2 : List[U]
fn std/list.filter_go -> List[T]
  param v0 : List[T] owned
  param v1 : fn(T) -> Bool !e owned
  param v2 : Int owned
  param v3 : List[T] owned
  block : List[T]
    loop L0
      return
        if : List[T]
          binary eq : Bool
            local v2 : Int
            intrinsic len : Int
              local v0 : List[T]
          block : List[T]
            local v3 : List[T]
          block : List[T]
            let v4 : T
              intrinsic expect : T
                intrinsic get : Option[T]
                  local v0 : List[T]
                  local v2 : Int
                str "index below length"
            block : Never
              let v5 : List[T]
                local v0 : List[T]
              let v6 : fn(T) -> Bool !e
                local v1 : fn(T) -> Bool !e
              let v7 : Int
                binary add : Int
                  local v2 : Int
                  int 1
              let v8 : List[T]
                if : List[T]
                  call dynamic : Bool
                    local v1 : fn(T) -> Bool !e
                    local v4 : T
                  block : List[T]
                    binary concat : List[T]
                      local v3 : List[T]
                      listlit : List[T]
                        local v4 : T
                  block : List[T]
                    local v3 : List[T]
              assign v0
                local v5 : List[T]
              assign v1
                local v6 : fn(T) -> Bool !e
              assign v2
                local v7 : Int
              assign v3
                local v8 : List[T]
              continue L0
fn std/list.filter -> List[T]
  param v0 : List[T] owned
  param v1 : fn(T) -> Bool !e owned
  block : List[T]
    let v2 : List[T]
      listlit : List[T]
    call direct std/list.filter_go : List[T]
      local v0 : List[T]
      local v1 : fn(T) -> Bool !e
      int 0
      local v2 : List[T]
fn std/list.fold_go -> A
  param v0 : List[T] owned
  param v1 : fn(A, T) -> A !e owned
  param v2 : Int owned
  param v3 : A owned
  block : A
    loop L0
      return
        if : A
          binary eq : Bool
            local v2 : Int
            intrinsic len : Int
              local v0 : List[T]
          block : A
            local v3 : A
          block : A
            block : Never
              let v4 : List[T]
                local v0 : List[T]
              let v5 : fn(A, T) -> A !e
                local v1 : fn(A, T) -> A !e
              let v6 : Int
                binary add : Int
                  local v2 : Int
                  int 1
              let v7 : A
                call dynamic : A
                  local v1 : fn(A, T) -> A !e
                  local v3 : A
                  intrinsic expect : T
                    intrinsic get : Option[T]
                      local v0 : List[T]
                      local v2 : Int
                    str "index below length"
              assign v0
                local v4 : List[T]
              assign v1
                local v5 : fn(A, T) -> A !e
              assign v2
                local v6 : Int
              assign v3
                local v7 : A
              continue L0
fn std/list.fold -> A
  param v0 : List[T] owned
  param v1 : A owned
  param v2 : fn(A, T) -> A !e owned
  call direct std/list.fold_go : A
    local v0 : List[T]
    local v2 : fn(A, T) -> A !e
    int 0
    local v1 : A
fn std/list.find_go -> Option[T]
  param v0 : List[T] owned
  param v1 : fn(T) -> Bool !e owned
  param v2 : Int owned
  block : Option[T]
    loop L0
      return
        if : Option[T]
          binary eq : Bool
            local v2 : Int
            intrinsic len : Int
              local v0 : List[T]
          block : Option[T]
            ctor 0/1 : Option[T]
          block : Option[T]
            let v3 : T
              intrinsic expect : T
                intrinsic get : Option[T]
                  local v0 : List[T]
                  local v2 : Int
                str "index below length"
            if : Option[T]
              call dynamic : Bool
                local v1 : fn(T) -> Bool !e
                local v3 : T
              block : Option[T]
                ctor 0/0 : Option[T]
                  local v3 : T
              block : Option[T]
                block : Never
                  let v4 : List[T]
                    local v0 : List[T]
                  let v5 : fn(T) -> Bool !e
                    local v1 : fn(T) -> Bool !e
                  let v6 : Int
                    binary add : Int
                      local v2 : Int
                      int 1
                  assign v0
                    local v4 : List[T]
                  assign v1
                    local v5 : fn(T) -> Bool !e
                  assign v2
                    local v6 : Int
                  continue L0
fn std/list.find -> Option[T]
  param v0 : List[T] owned
  param v1 : fn(T) -> Bool !e owned
  call direct std/list.find_go : Option[T]
    local v0 : List[T]
    local v1 : fn(T) -> Bool !e
    int 0
fn std/list.slice_go -> List[T]
  param v0 : List[T] owned
  param v1 : Int owned
  param v2 : Int owned
  param v3 : List[T] owned
  block : List[T]
    loop L0
      return
        if : List[T]
          binary ge : Bool
            local v1 : Int
            local v2 : Int
          block : List[T]
            local v3 : List[T]
          block : List[T]
            block : Never
              let v4 : List[T]
                local v0 : List[T]
              let v5 : Int
                binary add : Int
                  local v1 : Int
                  int 1
              let v6 : Int
                local v2 : Int
              let v7 : List[T]
                binary concat : List[T]
                  local v3 : List[T]
                  listlit : List[T]
                    intrinsic expect : T
                      intrinsic get : Option[T]
                        local v0 : List[T]
                        local v1 : Int
                      str "index below length"
              assign v0
                local v4 : List[T]
              assign v1
                local v5 : Int
              assign v2
                local v6 : Int
              assign v3
                local v7 : List[T]
              continue L0
fn std/list.clamp -> Int
  param v0 : Int owned
  param v1 : Int owned
  if : Int
    binary lt : Bool
      local v0 : Int
      int 0
    block : Int
      int 0
    block : Int
      if : Int
        binary gt : Bool
          local v0 : Int
          local v1 : Int
        block : Int
          local v1 : Int
        block : Int
          local v0 : Int
fn std/list.take -> List[T]
  param v0 : List[T] owned
  param v1 : Int owned
  block : List[T]
    let v2 : List[T]
      listlit : List[T]
    call direct std/list.slice_go : List[T]
      local v0 : List[T]
      int 0
      call direct std/list.clamp : Int
        local v1 : Int
        intrinsic len : Int
          local v0 : List[T]
      local v2 : List[T]
fn std/list.drop -> List[T]
  param v0 : List[T] owned
  param v1 : Int owned
  block : List[T]
    let v2 : List[T]
      listlit : List[T]
    call direct std/list.slice_go : List[T]
      local v0 : List[T]
      call direct std/list.clamp : Int
        local v1 : Int
        intrinsic len : Int
          local v0 : List[T]
      intrinsic len : Int
        local v0 : List[T]
      local v2 : List[T]
fn std/list.slice -> List[T]
  param v0 : List[T] owned
  param v1 : Int owned
  param v2 : Int owned
  block : List[T]
    let v3 : List[T]
      listlit : List[T]
    call direct std/list.slice_go : List[T]
      local v0 : List[T]
      call direct std/list.clamp : Int
        local v1 : Int
        intrinsic len : Int
          local v0 : List[T]
      call direct std/list.clamp : Int
        local v2 : Int
        intrinsic len : Int
          local v0 : List[T]
      local v3 : List[T]
fn std/list.reverse_go -> List[T]
  param v0 : List[T] owned
  param v1 : Int owned
  param v2 : List[T] owned
  block : List[T]
    loop L0
      return
        if : List[T]
          binary lt : Bool
            local v1 : Int
            int 0
          block : List[T]
            local v2 : List[T]
          block : List[T]
            block : Never
              let v3 : List[T]
                local v0 : List[T]
              let v4 : Int
                binary sub : Int
                  local v1 : Int
                  int 1
              let v5 : List[T]
                binary concat : List[T]
                  local v2 : List[T]
                  listlit : List[T]
                    intrinsic expect : T
                      intrinsic get : Option[T]
                        local v0 : List[T]
                        local v1 : Int
                      str "index below length"
              assign v0
                local v3 : List[T]
              assign v1
                local v4 : Int
              assign v2
                local v5 : List[T]
              continue L0
fn std/list.reverse -> List[T]
  param v0 : List[T] owned
  block : List[T]
    let v1 : List[T]
      listlit : List[T]
    call direct std/list.reverse_go : List[T]
      local v0 : List[T]
      binary sub : Int
        intrinsic len : Int
          local v0 : List[T]
        int 1
      local v1 : List[T]
fn std/list.sort -> List[T]
  param v0 : List[T] owned
  dict v1 : T owned
  intrinsic sort_by : List[T]
    local v0 : List[T]
    closure std/list.lambda$0 : fn(T, T) -> Int
      local v1 : T
fn std/list.best_by_go -> T
  param v0 : List[T] owned
  param v1 : fn(T) -> K !e owned
  param v2 : Int owned
  param v3 : T owned
  param v4 : K owned
  param v5 : Bool owned
  dict v6 : K owned
  block : T
    loop L0
      return
        if : T
          binary eq : Bool
            local v2 : Int
            intrinsic len : Int
              local v0 : List[T]
          block : T
            local v3 : T
          block : T
            let v7 : T
              intrinsic expect : T
                intrinsic get : Option[T]
                  local v0 : List[T]
                  local v2 : Int
                str "index below length"
            let v8 : K
              call dynamic : K
                local v1 : fn(T) -> K !e
                local v7 : T
            let v9 : Int
              call method trait=0 cmp slot=0 : Int
                local v6 : <dict>
                local v8 : K
                local v4 : K
            let v10 : Bool
              if : Bool
                local v5 : Bool
                block : Bool
                  binary gt : Bool
                    local v9 : Int
                    int 0
                block : Bool
                  binary lt : Bool
                    local v9 : Int
                    int 0
            if : T
              local v10 : Bool
              block : T
                block : Never
                  let v11 : List[T]
                    local v0 : List[T]
                  let v12 : fn(T) -> K !e
                    local v1 : fn(T) -> K !e
                  let v13 : Int
                    binary add : Int
                      local v2 : Int
                      int 1
                  let v14 : T
                    local v7 : T
                  let v15 : K
                    local v8 : K
                  let v16 : Bool
                    local v5 : Bool
                  assign v0
                    local v11 : List[T]
                  assign v1
                    local v12 : fn(T) -> K !e
                  assign v2
                    local v13 : Int
                  assign v3
                    local v14 : T
                  assign v4
                    local v15 : K
                  assign v5
                    local v16 : Bool
                  continue L0
              block : T
                block : Never
                  let v17 : List[T]
                    local v0 : List[T]
                  let v18 : fn(T) -> K !e
                    local v1 : fn(T) -> K !e
                  let v19 : Int
                    binary add : Int
                      local v2 : Int
                      int 1
                  let v20 : T
                    local v3 : T
                  let v21 : K
                    local v4 : K
                  let v22 : Bool
                    local v5 : Bool
                  assign v0
                    local v17 : List[T]
                  assign v1
                    local v18 : fn(T) -> K !e
                  assign v2
                    local v19 : Int
                  assign v3
                    local v20 : T
                  assign v4
                    local v21 : K
                  assign v5
                    local v22 : Bool
                  continue L0
fn std/list.max_by -> Option[T]
  param v0 : List[T] owned
  param v1 : fn(T) -> K !e owned
  dict v2 : K owned
  block : Option[T]
    let v3 : Option[T]
      intrinsic get : Option[T]
        local v0 : List[T]
        int 0
    let v4 : Option[T]
      intrinsic null_ref : Option[T]
    loop L0
      block : Unit
        sif
          isctor 0/1
            local v3 : Option[T]
          block : Never
            block : Never
              assign v4
                ctor 0/1 : Option[T]
              break L0
        sif
          isctor 0/0
            local v3 : Option[T]
          block : Never
            let v5 : T
              field 0/0.0 : T
                local v3 : Option[T]
            block : Never
              assign v4
                ctor 0/0 : Option[T]
                  call direct std/list.best_by_go : T
                    local v0 : List[T]
                    local v1 : fn(T) -> K !e
                    int 1
                    local v5 : T
                    call dynamic : K
                      local v1 : fn(T) -> K !e
                      local v5 : T
                    bool true
                    local v2 : <dict>
              break L0
        discard
          intrinsic panic : Never
            str "unreachable match"
    local v4 : Option[T]
fn std/list.min_by -> Option[T]
  param v0 : List[T] owned
  param v1 : fn(T) -> K !e owned
  dict v2 : K owned
  block : Option[T]
    let v3 : Option[T]
      intrinsic get : Option[T]
        local v0 : List[T]
        int 0
    let v4 : Option[T]
      intrinsic null_ref : Option[T]
    loop L0
      block : Unit
        sif
          isctor 0/1
            local v3 : Option[T]
          block : Never
            block : Never
              assign v4
                ctor 0/1 : Option[T]
              break L0
        sif
          isctor 0/0
            local v3 : Option[T]
          block : Never
            let v5 : T
              field 0/0.0 : T
                local v3 : Option[T]
            block : Never
              assign v4
                ctor 0/0 : Option[T]
                  call direct std/list.best_by_go : T
                    local v0 : List[T]
                    local v1 : fn(T) -> K !e
                    int 1
                    local v5 : T
                    call dynamic : K
                      local v1 : fn(T) -> K !e
                      local v5 : T
                    bool false
                    local v2 : <dict>
              break L0
        discard
          intrinsic panic : Never
            str "unreachable match"
    local v4 : Option[T]
fn std/list.max -> Option[T]
  param v0 : List[T] owned
  dict v1 : T owned
  call direct std/list.max_by : Option[T]
    local v0 : List[T]
    closure std/list.lambda$1 : fn(T) -> T
    local v1 : <dict>
fn std/list.min -> Option[T]
  param v0 : List[T] owned
  dict v1 : T owned
  call direct std/list.min_by : Option[T]
    local v0 : List[T]
    closure std/list.lambda$2 : fn(T) -> T
    local v1 : <dict>
fn std/list.lambda$0 -> Int
  capture v0 : T owned
  param v1 : T owned
  param v2 : T owned
  call method trait=0 cmp slot=0 : Int
    local v0 : <dict>
    local v1 : T
    local v2 : T
fn std/list.lambda$1 -> T
  param v0 : T owned
  local v0 : T
fn std/list.lambda$2 -> T
  param v0 : T owned
  local v0 : T
