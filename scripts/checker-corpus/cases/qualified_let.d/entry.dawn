# Qualified constructors in structural lets share match-pattern checking.
use lib as q

fn accepted(value: q.Box[Int]) -> Int = {
  let q.One(x) = value
  x
}

fn recovered(value: q.Box[Int]) -> Int = {
  let missing.One(y) = value
  y
}
