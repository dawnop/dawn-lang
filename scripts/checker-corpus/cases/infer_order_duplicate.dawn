# A duplicated name is done at its first completion: caller waits for the
# first twin only, so it is checked before the second one.
fn twin() = {
  let a: Int = "one"
  1
}
fn caller() = {
  let b: Int = "two"
  twin()
}
fn twin() = {
  let c: Int = "three"
  2
}
