# checker.dawn: `return` needs a function with a known return type
pub fn mismatch() -> Int = {
  return "a"
}

pub fn from_lambda() -> Int = {
  let f = () => { return 1 }
  2
}

fn inferred() = {
  return 1
}
