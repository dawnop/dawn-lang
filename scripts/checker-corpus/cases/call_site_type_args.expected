D	call_site_type_args.dawn	548	555	Dawn has no call-site type arguments: `id[Int]` reads as indexing the function `id`	a type parameter is bound only by the arguments and the expected type: call `id(...)` and annotate the binding that takes its result, or the one an argument comes from
D	call_site_type_args.dawn	599	608	Dawn has no call-site type arguments: `id[Point]` reads as indexing the function `id`	a type parameter is bound only by the arguments and the expected type: call `id(...)` and annotate the binding that takes its result, or the one an argument comes from
D	call_site_type_args.dawn	644	657	Dawn has no call-site type arguments: `id[List[Int]]` reads as indexing the function `id`	a type parameter is bound only by the arguments and the expected type: call `id(...)` and annotate the binding that takes its result, or the one an argument comes from
D	call_site_type_args.dawn	706	722	Dawn has no call-site type arguments: `catch_panic[Int]` reads as indexing the function `catch_panic`	a type parameter is bound only by the arguments and the expected type: call `catch_panic(...)` and annotate the binding that takes its result, or the one an argument comes from
D	call_site_type_args.dawn	763	773	`[]` needs an `Index` impl for `fn(Int) -> Int`	List and Map have one; when absence is a normal case, use get(xs, i) / map.get(m, k)
D	call_site_type_args.dawn	769	772	undefined constructor: Int	
D	call_site_type_args.dawn	819	822	undefined constructor: Int	
