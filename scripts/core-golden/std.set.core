module std/set
fn std/set.empty -> Set[T]
  intrinsic set_empty : Set[T]
fn std/set.from -> Set[T]
  param v0 : List[T] owned
  intrinsic set_from : Set[T]
    local v0 : List[T]
fn std/set.insert -> Set[T]
  param v0 : Set[T] owned
  param v1 : T owned
  intrinsic set_insert : Set[T]
    local v0 : Set[T]
    local v1 : T
fn std/set.remove -> Set[T]
  param v0 : Set[T] owned
  param v1 : T owned
  intrinsic set_remove : Set[T]
    local v0 : Set[T]
    local v1 : T
fn std/set.has -> Bool
  param v0 : Set[T] owned
  param v1 : T owned
  intrinsic set_has : Bool
    local v0 : Set[T]
    local v1 : T
fn std/set.size -> Int
  param v0 : Set[T] owned
  intrinsic set_size : Int
    local v0 : Set[T]
fn std/set.to_list -> List[T]
  param v0 : Set[T] owned
  intrinsic set_to_list : List[T]
    local v0 : Set[T]
fn std/set.show_go -> String
  param v0 : List[T] owned
  param v1 : Int owned
  param v2 : String owned
  dict v3 : T owned
  block : String
    loop L0
      return
        if : String
          binary eq : Bool
            local v1 : Int
            intrinsic len : Int
              local v0 : List[T]
          block : String
            local v2 : String
          block : String
            block : Never
              let v4 : List[T]
                local v0 : List[T]
              let v5 : Int
                binary add : Int
                  local v1 : Int
                  int 1
              let v6 : String
                binary concat : String
                  local v2 : String
                  binary concat : String
                    if : String
                      binary eq : Bool
                        local v1 : Int
                        int 0
                      block : String
                        str ""
                      block : String
                        str ", "
                    call method trait=3 show slot=0 : String
                      local v3 : <dict>
                      intrinsic list_index : E
                        local v0 : List[T]
                        local v1 : Int
              assign v0
                local v4 : List[T]
              assign v1
                local v5 : Int
              assign v2
                local v6 : String
              continue L0
fn std/set.show -> String impl_of=3/Set[T]
  param v0 : Set[T] owned
  dict v1 : T owned
  binary concat : String
    str "set_from(["
    binary concat : String
      call direct std/set.show_go : String
        call direct std/set.to_list : List[T]
          local v0 : Set[T]
        int 0
        str ""
        local v1 : <dict>
      str "])"
