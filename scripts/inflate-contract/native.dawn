# Native/JVM entry point for the pure gzip member contract. It is separate from
# probe.dawn because the independent Java compressor is deliberately unavailable
# to the native backend.

use std/io
use gzip_cases

pub fn main() -> Unit !io = io.println("mismatches " ++ to_string(gzip_cases.check()))
