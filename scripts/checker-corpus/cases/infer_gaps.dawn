# checker.dawn: places inference gives up
use std/list

pub fn empty_list() -> Int = {
  let xs = []
  1
}

fn recursive(n: Int) = if n == 0 { 1 } else { recursive(n - 1) }

pub fn ctor_value() -> Int = {
  let f = Some
  1
}
