fn sum_to(n: Int) -> Int = {
  let xs = range(0, n)
  var acc = 0
  for i in 0 ..len(xs) {
    acc = acc + match get(xs, i) { Some(v) -> v, None -> 0 }
  }
  acc
}

pub fn main() -> Unit !io = println("${sum_to(101)}")
