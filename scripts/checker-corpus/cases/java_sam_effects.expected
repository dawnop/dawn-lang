D	java_sam_effects.dawn	1203	1220	this function cannot cross to `Runnable`: its row !Log owes evidence	a Java caller has no call site to supply an evidence pack, so only a pure or `!io` function value converts (spec §9.4)\n  write the handler inside the function value, so that the row crossing is pure or `!io`:\n  () => { with handle Log { ... } ... }
D	java_sam_effects.dawn	1440	1456	this function cannot cross to `Consumer`: its row !Log owes evidence	a Java caller has no call site to supply an evidence pack, so only a pure or `!io` function value converts (spec §9.4)\n  write the handler inside the function value, so that the row crossing is pure or `!io`:\n  () => { with handle Log { ... } ... }
D	java_sam_effects.dawn	1611	1612	this function cannot cross to `Runnable`: its row !e owes evidence	a Java caller has no call site to supply an evidence pack, so only a pure or `!io` function value converts (spec §9.4)\n  an effect variable or an associated-effect projection may stand for a label, so it does not cross either; give this function value a pure or `!io` row
