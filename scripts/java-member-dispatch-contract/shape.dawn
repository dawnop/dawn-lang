# Parser ownership: after `.` an uppercase word is a member reference and
# nothing more. The AST must show a field access under an application -- the
# checker, not the parser, decides that this one is a method call (SYN-09).
use java "java.lang.Math"

pub fn remainder(x: Float) -> Float !io = Math.IEEEremainder(x, 3.0)
