# 序 6 调研：`Int`/`char` 值类型特化 + `String` 非物化字符访问

> 动码前的**调研与方案**，不是设计定稿。结论：**retro 对序 6 的描述与代码现状有实质出入**，
> 照原样做会推翻两条深思熟虑过的决策、且**对主要痛点收效甚微**。建议拆开做，先做便宜的那半。

## 一、结论先行

| | retro 的说法（`m6-retro.md:61-74`、`:261`） | 代码与实测的现状 |
|---|---|---|
| `Int` 装箱 | 「泛型全擦除 + 装箱」，暗示 Int 到处是堆对象 | **局部变量/参数/返回值/具体记录字段/闭包捕获全是 `J`（原生 long），零装箱**。装箱只发生在**声明位置是类型变量**的地方 |
| `char = Int` | 列为缺陷「把 char 变成堆上的 Int」 | **`char` 根本不是堆对象**——它就是 `Int`，即原生 `long`。Go 的 rune 模型，spec §1.5 明写、`CharTest.kt` 钉住，是**刻意设计** |
| 「一篇文章正文 = 百万个装箱 Int」 | 主要机制 | **对 ASCII 不成立**（`Long.valueOf` 缓存 −128..127）。实测装箱只占 **+40%**；大头是 `ArrayList` 物化本身 |
| 修法 | 「值类型特化」 | **对 ASCII 一分钱不省**：`long[]` 的 8 字节 == 现在的指针 8 字节。真正省的是**不物化** |

**建议**：把序 6 拆成两件事，只做 **A（String 非物化访问，库层）**，把 **D（单态化/值类型特化，编译器大改）
无限期搁置**，除非基准证明 A 之后还有不可接受的缺口。**B（元组特化）**便宜且独立，可顺手做。

## 二、现状（三份调研的交集，均有 file:line 佐证）

### 2.1 装箱策略就是一行

`CodeGen.kt:645` —— `adaptTo(actual, declared) = if (declared is TVar) box(actual)`。
**值本身是不是具体类型无关紧要，只看声明位置。** 于是：

| 位置 | `Int` 的描述符 | 装箱 |
|---|---|---|
| 局部变量 / 参数 / 返回值 | `J`（LLOAD/LSTORE） | **否** |
| **具体**记录/ADT 字段 | `J` | **否** |
| 闭包捕获 | `J` | **否** |
| `List[Int]` 元素、Map 键值 | `Object` | 是 |
| **元组分量（永远）** | `Object` | 是 |
| `Option[Int]`/`Result` 载荷 | `Object` | 是 |
| 泛型函数实参、trait 字典调用、`Fn1.apply` | `Object` | 是 |

即：**剩下的装箱恰好等于「泛型位置的装箱」，而那正是 D3 与 D9 明确选择的东西**
（D3「泛型走独立的、无聊的参数多态（擦除 + 装箱）」；D9「不做单态化——native-image
代码膨胀 + 编译时间不划算」）。D6 还专门写着「为想象中的需求预付架构成本是小项目最常见的死法」。
**序 6 原样做 = 推翻 D3 + D9，并顶着 D6。** 门槛必须高。

### 2.2 真正的缺口是 `String` 没有 `char_at`

`Bytes` 有 `byte_at`/`byte_slice`/`byte_index_of`（`Types.kt:454-467`），**`String` 一个都没有**。
要读一个字符只有两条路：`substring(s,i,i+1)`（返回 `String` 不是 `Int`），或把整个串炸成 `List[Int]`。

**这个不对称就是全部痛点的来源**，而且**序 4（Bytes）已经用同样的手法解决过一次**：
retro 引用的 `multipart.dawn` 那句警告（"never code_points on the content ... would box millions of Ints"）
**现在已经不存在了**——`f9c339e` 加了 `byte_index_of`/`byte_slice` 之后，multipart 改成在 Bytes 上定位结构，
**没动类型系统**。序 6 的 String 那半，是同一个剧本的重演。

### 2.3 受害现场（按输入规模分三档）

- **档 1（真疼，整篇正文）**：`json/parser.dawn:126` `parse` 把**整个 JSON 文档**炸成 `List[Int]` 才开始词法分析
  （**retro 里根本没提，却是最热的路径**——`POST /api/articles` 的正文在 JSON 里，要付两遍）；
  `repo_article.dawn:14` `word_count`（且在 `:72` 按行调用 → `GET /api/articles?size=10` 物化**十篇正文**只为算个整数）；
  `search.dawn` 的 `strip_md`/`excerpt_html` 链。
- **档 2（小输入但每字符分配一个 List）**：`export.dawn:22,33`、`search.dawn:158`。
- **档 3（装箱无关，纯粹是没有 `char_at`）**：`needs_quote`、`slugify`、`only_slug_chars`、
  以及所有 `from_code_points([13,10])` 拼 CRLF。**这一档只要 API 补齐就消失，与性能无关。**

顺带两个独立的人体工学缺口：`index_of` **没有 `from` 参数**（逼出 `search.dawn:166` 手写版）、
`replace`/`slice` **不是内建**（每个应用各手写一遍）。

## 三、实测（本轮亲自跑，非推测）

基准载体：`dawn run examples/m4/json <file>`，纯 Dawn、多模块、正打在 `code_points` 上。

**1) 物化的代价是数量级的**：1.3MB JSON → **peak RSS 467–500MB**（≈ **360× 输入**）。

**2) 装箱只是其中一小部分**——两份语料**码点数完全相同**（902901），只差码点值：

| 语料 | peak RSS（3 次） | 机制 |
|---|---|---|
| 正文全 ASCII（码点 < 128） | 453 / 478 / 583 MB | **命中 `Long` 缓存，一个 Long 都没分配** |
| 正文全中文（码点 > 19967） | 633 / 744 / 727 MB | 每字符新分配一个 16 字节 `Long` |

**读法**：装箱带来 **+40%**；但**纯 ASCII 一个 Long 都没装箱，仍然烧掉 453MB**。
大头是 `ArrayList` 物化本身——90 万个元素的指针骨架 + 扩容倍增拷贝 + GC 余量。

**3) 这一条直接判了「值类型特化」的死刑**：把 `List[Int]` 特化成 `long[]` 后，每码点 **8 字节**——
**和现在的指针一样是 8 字节**。也就是说**特化对 ASCII 场景省下的是零**，对中文省掉那 16 字节的 Long。
而**不物化**省掉的是**全部**。retro 把因果归错了：痛的不是"装箱"，是"物化"。

## 四、方案

### 方案 A —— `String` 非物化字符访问（**推荐**，库层，不动类型系统）

补齐 String 侧的 `Bytes` 对等物，让档 1/2/3 的调用点都不再需要 `code_points`。与 D3/D6/D9 全部相容，
是序 4 已验证过的形状。

**待定的设计题（A 的真正难点，不是工作量而是 API 形状）**：Java `String` 是 UTF-16，
而 Dawn 现有的 `str_len`/`substring`/`index_of` **全部按码点索引**（`CodeGen.kt:2104-2113` 做 UTF-16↔码点换算）。
于是「按码点索引的 `char_at(s, i)`」是 **O(n)**，循环里用就是 **O(n²)**——比现在还糟。三个候选：

- **A1 游标**：`next_char(s, cursor) -> Option[(Int, Int)]`（码点 + 下一个游标）。游标对用户不透明，
  实为 UTF-16 下标。O(1) 每步，能满足 JSON 词法/回溯。**代价**：返回元组 → 撞上元组装箱（见方案 B）。
- **A2 回调遍历**：`each_char(s, f)` / `chars_fold(s, init, f)`。最简单、O(n)、零物化，
  但**不支持回溯**，JSON 词法器用不了。
- **A3 暴露 UTF-16**：`char_at_utf16` + `utf16_len`。O(1)、无元组，但把编码模型漏进语言，
  与现有全码点索引的 API 精神冲突。

倾向 **A1**（唯一能覆盖档 1 里 JSON 词法器的），且它给方案 B 提供了独立的动机。
**顺手补**：`index_of(s, sub, from)`、`replace`、`slice` —— 消掉三处手写实现。

**收益**：预计把 JSON 基准的 467MB 打到接近输入规模。**成本**：中。**风险**：低（纯新增，旧 API 不动）。

### 方案 B —— 元组特化（便宜、独立、修一个真不一致）

`genTupleClass(n)` **只按元数生成类**，字段硬编码 `Object`（`CodeGen.kt:438`）——它连类型都拿不到。
而 `genCtorClass` 用 `descOf(f.type)`（`:701`），**具体记录的字段是全原生的**。
于是 `type P = { x: Int, y: Int }` 零装箱，`(Int, Int)` 两个装箱——**同样的类型，两种待遇**。

改法：元组类按**类型形状**生成（`Tuple2$JJ`），镜像 `genCtorClass` 的现成做法。类数量受实际用到的形状数约束。
**如果走 A1，这条是前置**（游标返回元组，不特化就白省）。**成本**：低-中。**风险**：低。

### 方案 C —— 窄化的容器/字典特化

如 `Ord[Int].cmp` 现在每次比较要拆两个 Long 箱（`CodeGen.kt:979`，底下其实是条 `LCMP`）；
或给 `map`/`fold` 加原生特化。**收益零散、且 A 之后大概率不再是瓶颈**。**建议：不做，等基准说话。**

### 方案 D —— 真·值类型特化 / 单态化

推翻 D3 + D9，顶着 D6。**且第三节的实测显示它对 ASCII 场景收益为零**。
**建议：无限期搁置**，除非 A+B 落地后基准仍显示不可接受的缺口——那时再谈，也有基准可依。

## 五、建议路线

1. **第 0 步（前置，retro 自己要求的"性能基准护栏"）**：`scripts/bench.sh` —— `dawn run examples/m4/json`
   跑生成的语料，`/usr/bin/time` 取 **peak RSS 中位数**（wall 噪声大：0.48–0.71s；RSS 是 460MB 量级，信噪比极高）。
   录基线、超阈值报警。约半天。**没有它，A 的收益和 D 的必要性都只是嘴上说。**
2. **A1 定稿**（游标 API 的形状是唯一真设计题）→ 落地 A + 顺手补 `index_of(from)`/`replace`/`slice`。
3. **B（元组特化）**——若 A1 定为游标则它是前置，否则可独立择机。
4. **重测**。若缺口已可接受 → 序 6 **结案，D 记入"被实测推翻的方案"**，与 `@NonNull`、`List[T]→T[]` 桥并列。

## 五之补 —— 路线执行到哪了（2026-07-18）

### 第 0 步：完成，基准脚本 `scripts/bench.sh`（**量法已于同日更正，见下**）

量的是**建成的 jar**（不是 `dawn run`）跑确定性生成的语料。测 `dawn run` 会把编译器自己的
占用折进被监视的数字里。

**第一版量 peak RSS，那是错的**——见 §五之补末尾「基准量法被自己推翻」。现在量的是
**二分找到的最小可运行堆**：

| 语料 | 最小堆 | 倍率 |
|---|---|---|
| 905 KB | 13 MB | 14× |
| 2719 KB | 37 MB | 13× |

**基准就在这里，改动想动这个数就回来改它**——否则回归和收益分不开。

### 第 2 步：A1 落地，且**它不需要 B 作前置**——原文的依赖判断是错的

原文写「A1 游标 → 返回元组 → 撞上元组装箱 → 故 B 是前置」。**元组来自那个签名，不来自游标概念。**
把一次返回拆成两个 O(1) 查询,元组和 Option 一起消失：

```dawn
cursor_start / cursor_end / cursor_done / cursor_char / cursor_next / cursor_prev
cursor_slice(s, from, to)          # 按游标切，不做下标换算
index_of_from(s, sub, from)        # 游标进，游标出
```

代价是每字符两次调用而非一次，换掉的是每字符一次 Tuple2 分配加两次 Long 装箱。
**于是 B 从「A1 的前置」降级为「独立的、可择机做的一致性修复」**——它修的那个不一致
（元组字段硬编码 `Object`，而具体记录的字段是全原生的）依然真实,只是不再挡路。

`cursor_char` 返回 -1 而非 `Option[Int]` 是同一笔账：热路径上每字符一次 `Some` 分配，
比它避免的哨兵约定贵得多。`index_of` 早已用同样的理由走 -1（FFI 边界过不了 ADT）。

### 落地验证：dawnop-site 的 `search.dawn`

真实调用点、真实文章。同一进程内 A/B（同样的 JIT 预热与 GC 压力），输出 checksum 逐字节一致：

| 文章长度 | 码点下标 | 游标 | 倍数 |
|---|---|---|---|
| 5.7k 字 | 93 ms | 11 ms | 8.5× |
| 23.5k 字 | 990 ms | 40 ms | 25× |
| 47k 字 | 2583 ms | 108 ms | 24× |

倍数**随规模增长**，这正是渐进差而非常数差的签名：旧版输入翻倍时间涨 2.6–3.6 倍，新版基本线性。

顺带确认了第 173 行那句话的力度：生产代码**自己手写了一份 `index_of_from`**，因为 std 没有——
而手写版每次调用都要 `code_points(整篇文章)`。**「标准库缺一个原语」的代价不是慢一点，是调用方
写出一个更慢的替代品。**

### 第 3 步：JSON 例子已迁到游标——**收益远小于本文预测，方案 A 的前提也得改**

`lexer.dawn` / `parser.dawn` 的位置从 `code_points(input)` 的下标改成游标（`site/src/json/` 的
vendored 副本同步）。JSONTestSuite **318 个用例判定逐条不变**，3 个单元测试不变。

wall 0.28s → 0.23s。内存收益小（迁移前后的最小堆分别是 42 MB 与 37 MB，约 −12%）。
**本文第四节写「预计把 JSON 基准打到接近输入规模」——这是错的**，但错的程度比我一开始
以为的小得多，见下。

按停止点分解（2719 KB 语料，**最小可运行堆**）：

| 停在哪 | 最小堆 |
|---|---|
| 只读文件 | 19 MB |
| + `code_points` | 27 MB |
| + 完整 parse | 39 MB |

**物化约值 8 MB，解析树约值 12 MB**（2.7 MB 输入的 ~4.4 倍）。一个装箱对象图是这个量级，
不算异常。方案 A 省掉的是那 8 MB 里的大部分——真实、但不足以让基准回到输入规模。

### 基准量法被自己推翻（同日，务必读）

上面这些数**第一版全是错的**，因为量的是 **peak RSS**：
`只读 62 / +code_points 89 / +parse 259 MB`，据此我写下「解析树占 197 MB，是文档的 73 倍」，
并断言「真缺口在表示层」。

**峰值 RSS 不度量程序需要多少内存，只度量 GC 被给了多大空间。** 默认 `-Xmx` 是内存的四分之一，
JVM 填满再收，于是 RSS 反映的是「JVM 懒得回收」。二分 `-Xmx` 一问便知：同一个程序
**39 MB 就跑得完**，GC 日志显示每次年轻代回收后存活 12–25 MB。**6.6 倍的虚高。**

连带作废的还有：站点生成器「700 KB → 104 MB」（真实最小堆 **11 MB**）、
本文第三节的 `ASCII 453 MB / 中文 633 MB / 1.3 MB 输入 → 360×`（同为峰值 RSS）。
**第三节的*比较*结论（同条件对比，装箱只占 +40%、ASCII 零装箱仍烧掉大头）不受影响，
但所有绝对倍数都要打折看。**

教训不是「峰值 RSS 没用」，是**它回答的问题不是我以为的那个**。改用最小可运行堆之后，
「表示层 73×」这个说法消失了——它从来不是一个 73 倍的问题。

**结论**：方案 A 值得做（`search.dawn` 那种流水线场景 24× 是时间上的真收益），
但它在内存上的收益是个位数 MB。第 4 步「若缺口已可接受则序 6 结案」——**按更正后的数据，
缺口本来就没有大到需要 D 的程度，这正是第三节一开始的结论。**

### 还没做

- ~~**表示层**（新出现的大头）：`Json` 树 73× 的开销。~~ **这条基于错误的量法，已撤销**：
  按最小可运行堆，解析树约 12 MB / 2.7 MB 输入，是普通装箱对象图的量级，没有「新出现的大头」。
- **B（元组特化）**：见上，不再是前置。
- ~~`replace` / `slice`：原文列的「顺手补」还剩这两个。~~ **已补（2026-07-22）**：
  `str.replace`（split+join 语义，空 `from` = 字符间拼接）与 `list.slice`
  （`[from, to)`，两端钳制，复用 take/drop 的 slice_go），golden
  `stdlib_replace_and_slice` 锁行为。

## 六、调研的附带发现（与序 6 无关，但都是真问题）

- **`dawn run site` 在 HEAD 上是坏的**：三处独立断裂，全是**文档漂移过了硬编码的 lint**
  （`md/parser.dawn:155` 不认 `####` 而 `spec.md:734` 新增了一个；`gen/common.dawn:52` 的文档链接白名单没登记
  `m6-retro.md`；`gen_api` 需要 `site/build/builtins.json` 而只有 `site/build.sh` 会造它）。
- **没有任何 CI**（`.github/workflows/` 不存在）。上面那个站点腐烂**就是这么无声上线的**。
  测试跑完只要 **1.74s**，接 CI 几乎零成本。
- **README:99 的两个数字都是陈的**：「测试 748 项」（实为 1142）、「native 启动约 7ms」（无任何 harness 支撑）。

---

## 附:实测数据(2026-07-18 补)

上文 §方案A 断言「按码点索引的 `char_at(s, i)` 是 O(n)」是对的。这里补上量出来的数,
以及一条**看起来能绕过去、实测反而更慢**的捷径——免得下次有人(包括作者)再试一遍。

### A. 三层模型:别把「存储」和「下标空间」混为一谈

| 层 | 单位 | `"héllo🎈"` |
|---|---|---|
| 存储(JDK 9+ compact strings) | `byte[]` + coder,**LATIN1 或 UTF16 二选一** | 含 🎈 → UTF16 |
| `java.lang.String` API | **UTF-16 码元** | `length()` = 7 |
| Dawn 模型(spec §1.5) | **码点** | `str_len` = 6 |

存储实测(各 100 万字符):ASCII **1.05 字节/字符**(LATIN1)、中文 **2.10 字节/字符**(UTF16)。

**关键**:存储层的 LATIN1 优化**不会**让下标换算变便宜——API 契约仍是 UTF-16 下标,
compact strings 纯属内部省内存。换算成本是**中间那层**制造的。

### B. 换算成本,以及方向的不对称

100 万字符的串上做 20000 次换算:

| | ASCII | 含星际码点 |
|---|---|---|
| 码点下标 → UTF-16 偏移(`offsetByCodePoints`) | **2568 ms** | 11662 ms |
| UTF-16 偏移 → 码点下标(`codePointCount`) | **0~1 ms** | — |

**返回码点下标便宜(Latin-1 有快路径),接受码点下标极贵(没有快路径)。**
所以任何**接受**码点下标的随机访问 API 都是陷阱——这正是 A1 用游标而不是下标的理由。

### C. 一条诱人的捷径,实测是错的

既然 LATIN1 串里码点下标 == 码元偏移,那加个守卫不就 O(1) 了?

```java
if (s.codePointCount(0, n) == n) return cpIndex;   // 无代理对 → 恒等
return s.offsetByCodePoints(0, cpIndex);
```

实测(同上规模):

| 串 | 加守卫 | 原始 |
|---|---|---|
| ASCII | **0 ms** | 2569 ms |
| 中文(BMP,无代理对) | 4117 ms | 12973 ms |
| **含 1 个星际码点** | **17141 ms** | 11736 ms |

ASCII 上白捡,但**星际码点上慢 46%**——守卫先 O(n) 扫一遍,发现有代理对,然后**还是要**
O(n) 换算,**两遍扫描**。而「这个串有没有代理对」我们**无法在 O(1) 内知道**。

→ **不要把这个守卫塞进原语**。它把罕见情况的代价变得更糟且不可预测。
真正的解法仍是 A1:**判断/解码只做一次**,之后每步 O(1),对所有文字一致。

### D. 生产现场佐证(dawnop-site `backend-dawn/src/search.dawn`)

markdown 清洗(给全站搜索建索引)**自己手搓了 `index_of_from`**——正是 §方案A 里
「顺手补 `index_of(s, sub, from)`」那一条。**用户在缺失处自己造轮子,是该补什么的最好证据。**

而手搓版有两处代价,都已量过并修掉其一(20KB 文章 / 60 次调用):

| | 耗时 |
|---|---|
| 原样(每次调用重新 `code_points` 整篇 + `eq_at` 用 `fold(range(..))`) | 0.09 s |
| 只改 `eq_at` 为递归(去掉每候选位置的 range 分配,且 `&&` 恢复短路) | **0.04 s** |
| 再把物化提到循环外 | ~0.02 s |

已修 `eq_at`(dawnop-site `7c55cc0`)。**物化仍在每次调用里**——彻底修需要把整条
清洗流水线改成在 `List[Int]` 上跑,属 A1 落地后的自然产物,没有单独去动。

> 注意 `eq_at` 那个 `fold(range(0, len(ncps)), true, fn(acc, k) => acc && ...)`:
> `&&` 短路的是**表达式**,不是 fold 的**迭代**——首字符不匹配仍会把整个 needle 比完。
> 这是个容易重犯的写法。
