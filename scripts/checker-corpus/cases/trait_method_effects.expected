D	trait_method_effects.dawn	567	583	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	trait_method_effects.dawn	1132	1133	`m` is declared !Ask but trait `Plain` declares it pure	drop !Ask here, or declare the trait method !Ask
D	trait_method_effects.dawn	1163	1164	`n` binds 1 effect variable(s) but trait `Plain` binds 0	they are paired in the order they are written; trait `Plain` declares it as fn n(Sub) -> Int, this impl declares fn n(Sub) -> Int !e
D	trait_method_effects.dawn	1716	1732	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	trait_method_effects.dawn	1702	1704	`sq` does not match the trait's signature	trait `Sigs` declares it as fn sq(Sub) -> Int
D	trait_method_effects.dawn	1750	1752	`sr` does not match the trait's signature	trait `Sigs` declares it as fn sr(Sub) -> Int
D	trait_method_effects.dawn	2221	2223	`tw` does not match the trait's signature	trait `Two` declares it as fn tw(Sub, fn() -> Int !b, fn() -> Int !a) -> Int !b
