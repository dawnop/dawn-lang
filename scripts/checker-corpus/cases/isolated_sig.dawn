# checker.dawn: leaving a comptime block puts the enclosing signature back
# (Frame.current_sig) -- without it `return` no longer knows what it is in
pub fn returns_wrong() -> Int = {
  let n = comptime { 2 }
  return "not an Int"
}
