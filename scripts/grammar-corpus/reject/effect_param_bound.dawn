# expect: an effect parameter cannot be constrained
# A trait bound constrains a type. There is nothing on the effect axis for one
# to say, so the syntax is refused rather than parsed and ignored.
fn f[!e: Show](x: Int) -> Int = x
