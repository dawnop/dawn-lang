# The surface diagnostic must come first: the header is checked before any
# body, so an unrelated body failure cannot reorder or suppress it.
type Secret = Secret

pub fn leak() -> Secret = Secret

pub fn unrelated() -> Int = "not an int"
