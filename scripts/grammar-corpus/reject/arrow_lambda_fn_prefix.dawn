# expect: a lambda has no `fn` prefix
# The `fn` prefix is retired everywhere except the trailing-closure position
# (spec §4.5). The diagnostic teaches the surviving spelling instead of
# reporting whatever the token after `fn` happens to break.
fn f(xs: List[Int]) -> List[Int] = map(xs, fn(x) => x + 1)
