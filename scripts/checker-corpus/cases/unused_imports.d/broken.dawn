# A module with a real error: its own unused import is not reported, because
# the use set of a module that failed to check is not known to be complete.
use std/list

pub fn value() -> Int = 1

fn wrong() -> Int = "not an Int"
