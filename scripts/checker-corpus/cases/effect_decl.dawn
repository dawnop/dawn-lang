# checker.dawn: effect declaration collisions
type Named = X

effect Named { fn a() -> Int }

effect Mine { fn a() -> Int }

effect Mine { fn b() -> Int }

effect Twice {
  fn op() -> Int
  fn op() -> Int
}
