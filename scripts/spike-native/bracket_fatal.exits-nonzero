This program is supposed to die: a panic crosses a bracket with nothing left
to catch it. Without this marker run.sh reads the non-zero exit as a failed
run and blocks the three checks that are the point of the file -- stdout up to
the failure, and the stderr and exit code the two backends have to agree on.
See the header of run.sh.
