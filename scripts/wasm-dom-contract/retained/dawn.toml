# A deliberately tiny retained-state reactor. The contract beside it drives
# one JVM process and one wasm module instance through the same multi-turn
# session, including a failed replacement.
schema = 1
name = "tea_dom_retained_contract"
version = "1.0.0"

[deps]
tea_core = "../../../packages/tea-core"
tea_dom = "../../../packages/tea-dom"
