D	local_fn.dawn	225	230	`inner` cannot introduce the effect variable `!e`	a local function's row can name the enclosing function's effect variables, but not bind new ones; lift `inner` to the top level for that
D	local_fn.dawn	325	328	the body of `inner` has type String but the declared return type is Int	
D	local_fn.dawn	369	374	the body of `inner` performs io	declare it: fn inner(...) -> Unit !io = ...
D	local_fn.dawn	447	452	the body of `local` performs io	declare it: fn local(...) -> (fn() -> Int) !io = ...
D	local_fn.dawn	919	922	`inner` uses the effect `Ask`, which its row does not declare	declare it: fn inner(...) -> T !Ask, or handle it inside its body with `with handle Ask { ... }`
D	local_fn.dawn	1228	1231	`inner` uses the effect `Ask`, which its row does not declare	declare it: fn inner(...) -> T !Ask, or handle it inside its body with `with handle Ask { ... }`
D	local_fn.dawn	2334	2339	`probe` uses the effect variable `!e`, which its row does not declare	declare it: fn probe(...) -> T !e
D	local_fn.dawn	2733	2736	`probe` uses the effect projection `!T.E`, which its row does not declare	declare it: fn probe(...) -> T !T.E
D	local_fn.dawn	3395	3400	the body of `inner` performs io	declare it: fn inner(...) -> Int !(Ask|io) = ...
