fn or_default[T](o: Option[T], d: T) -> T =
  match o {
    Some(v) -> v
    None -> d
  }

pub fn main() -> Unit !io = {
  println("${or_default(None, 5)}")
  println(or_default(Some("hi"), "bye"))
}
