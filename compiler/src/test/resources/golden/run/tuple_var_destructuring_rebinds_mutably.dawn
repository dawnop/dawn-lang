fn step(p: (Int, Int)) -> (Int, Int) = {
  let (a, b) = p
  (b, a + b)
}

pub fn main() -> Unit !io = {
  var (x, y) = (0, 1)
  while x < 20 {
    let (nx, ny) = step((x, y))
    x = nx
    y = ny
  }
  println("$x $y")
}
