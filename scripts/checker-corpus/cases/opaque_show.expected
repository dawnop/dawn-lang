D	opaque_show.dawn	603	607	cannot derive Show for `Holder`: field `h` of type Handle is not printable	a field needs an `impl Show` (or one of the type's own parameters)
D	opaque_show.dawn	719	730	no impl of `Show` for `Buf`	an opaque type does not inherit its target's `Show`: printing it would print the representation it hides; write `impl Show` for `Buf` in the module declaring it, or convert to the target there first
D	opaque_show.dawn	769	778	no impl of `Show` for `Handle`	an opaque type does not inherit its target's `Show`: printing it would print the representation it hides; write `impl Show` for `Handle` in the module declaring it, or convert to the target there first
D	opaque_show.dawn	826	837	no impl of `Show` for `Handle`	an opaque type does not inherit its target's `Show`: printing it would print the representation it hides; write `impl Show` for `Handle` in the module declaring it, or convert to the target there first
