fn or_zero(o: Option[Int]) -> Int =
  match o {
    Some(v) -> v
    None -> 0
  }

pub fn main() -> Unit !io = {
  println("${or_zero(Some(41)) + 1}")
  let n: Option[Int] = None
  println("${or_zero(n)}")
}
