# checker.dawn: leaving a comptime block puts the enclosing scope chain back
# (Frame.scopes) -- `outer` is still in scope after it, `nope` never was
pub fn after_comptime() -> Int = {
  let outer = 1
  let inner = comptime { 2 }
  let anchor = nope
  outer + inner + anchor
}
