# The actionable alias hint is limited to one nullary builtin constructor.
# Nominal sums and payload constructors retain the generic collision, while a
# declaration-name collision wins before constructor inspection.
type Sum = Other | Char
type Box = Char(value: Int)
type Int = Char
