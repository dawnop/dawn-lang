This program is supposed to die: `m[k]` on an absent key panics, and nothing
here catches it. Without this marker run.sh reads the non-zero exit as a
failed run and blocks the three checks that are the point of the file -- the
stdout up to the failure, and the stderr and exit code the two backends have
to agree on. See the header of run.sh.
