-- the reader walks past a node that is not its own --
43
-- a pure reader under the same chain, which follows nothing --
4
-- the same key twice in one chain, frontmost first --
6
-- and both keys, so the walk has to find two different nodes --
46
