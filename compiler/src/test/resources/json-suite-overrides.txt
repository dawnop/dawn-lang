# JSONTestSuite files whose expected y_/n_ verdict Dawn intentionally flips, one
# per line as: <filename> <reason>. Lines starting with # are ignored. Kept small
# on purpose (the suite harness caps this at 5) — every entry needs a real reason.
