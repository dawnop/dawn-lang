# expect: expected an expression, found `=`
# Recovery must cross nested (), [], and {} without stopping on their newlines
# or commas, then resume at the following top-level arm and declaration.

fn f() = match x {
  A -> [
    Point {
      x: bad(
        =
      )
    },
  ]
  B -> 2
}

fn g() = 3
