D	builtin_alias_hint.dawn	182	201	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasInt = ...` instead
D	builtin_alias_hint.dawn	202	223	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasChar = ...` instead
D	builtin_alias_hint.dawn	224	247	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasFloat = ...` instead
D	builtin_alias_hint.dawn	248	269	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasBool = ...` instead
D	builtin_alias_hint.dawn	270	295	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasString = ...` instead
D	builtin_alias_hint.dawn	296	319	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasBytes = ...` instead
D	builtin_alias_hint.dawn	320	341	`type` declares a nominal type (constructors or a record); this is an alias	write `alias AliasUnit = ...` instead
