-- the arm's residual label comes from where it was installed --
outer log 1
7
-- and the raise site's own `log` still finds the inner one --
inner log 2
-- one more frame out, so the raise is not even in this body --
outer log 1
outer log 1
14
