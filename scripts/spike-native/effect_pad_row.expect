-- a closure that tells, into a slot whose concrete part is io --
  run: before
  run: after
43
-- short of a named atom as well as of the io --
  run2: before
165
-- a caller's own polymorphic callback, forwarded --
  run: before
  run: after
61
-- the control: a pure closure, and the keys nobody reads --
  run2: before
144
-- one short argument and one not, joining on one variable --
  both: before
  u: doing io
162
-- the nearest handler answers a padded pack, twice over --
  run: before
  run: after
89
  run: before
  run: after
97
