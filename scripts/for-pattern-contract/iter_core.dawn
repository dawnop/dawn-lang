type Pair = Pair(left: Int, right: Int)

fn source() -> List[Pair] !io = {
  println("source")
  [Pair(1, 2), Pair(3, 4)]
}

pub fn main() -> Unit !io = {
  var total = 0
  for Pair(left, right) in source() {
    total = total + left + right
  }
  println("total=$total")
}
