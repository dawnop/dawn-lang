# checker.dawn: alias arity, self-reference, and effect variables in a type
alias Loop = Loop

alias Pair[A, B] = (A, B)

pub fn a(x: Pair[Int]) -> Int = 1

type Carrier[T] = C(f: fn() -> T !e)

alias Later[T] = List[T]

pub fn b(x: Later[Int, Int]) -> Int = 1
