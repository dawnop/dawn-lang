# expect: an associated effect member has no body
# expect: expected `=` (function body), found `}`
# expect: expected `=` (function body), found `\\n`
trait P[T] {
  effect E { fn ask() -> Int }
  fn run(x: T) -> Int
}
