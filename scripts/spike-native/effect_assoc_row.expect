-- a projection in a parameter row, written inline --
111
-- the same row written through a transparent alias --
221
-- one signature instantiated at two different labels --
322
326
-- a named value crossing into the projection's slot --
1088
-- and the pack carried into a primitive's use closure --
1165
-- a binding that is pure: the empty pack, and the control --
417
-- a binding that is io: the other empty pack --
loud
io thunk
519
-- forwarded through a second signature without grounding --
727
-- a written label beside the projection in one row --
836
-- and the trait default body, through the dictionary bridge --
1766
