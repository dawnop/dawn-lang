D	question_with.dawn	210	212	`?` after `with` needs the type of the `with` call to be known from context	annotate the enclosing function's return type, so the closure `with` introduced knows what it hands back
D	question_with.dawn	200	213	cannot infer type parameter(s) T for `poly`	Dawn has no call-site type arguments, so the type has to arrive as an expectation: annotate the binding an argument comes from, or the one that takes the result
