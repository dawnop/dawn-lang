# checker.dawn: selective `use` -- private names, absent names, and an alias
# asked to name two modules
use lib.{secret}
use lib.{nosuch}
use lib.{Hidden}
use lib.{Nope}
use lib as m
use other as m
use std/nosuchmodule
