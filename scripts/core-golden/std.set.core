module std/set
fn std/set.empty -> Set[T]
  call direct std/hamt.empty : Hamt[K, V]
fn std/set.from -> Set[T]
  param v0 : List[T] owned
  dict v1 : T owned
  dict v2 : T owned
  call direct std/hamt.sfrom : Hamt[T, Bool]
    local v0 : List[T]
    local v1 : <dict>
    local v2 : <dict>
fn std/set.insert -> Set[T]
  param v0 : Set[T] owned
  param v1 : T owned
  dict v2 : T owned
  dict v3 : T owned
  call direct std/hamt.sadd : Hamt[T, Bool]
    local v0 : Set[T]
    local v1 : T
    local v2 : <dict>
    local v3 : <dict>
fn std/set.remove -> Set[T]
  param v0 : Set[T] owned
  param v1 : T owned
  dict v2 : T owned
  dict v3 : T owned
  call direct std/hamt.dissoc : Hamt[K, V]
    local v0 : Set[T]
    local v1 : T
    local v2 : <dict>
    local v3 : <dict>
fn std/set.has -> Bool
  param v0 : Set[T] borrowed
  param v1 : T owned
  dict v2 : T owned
  dict v3 : T owned
  call direct std/hamt.has : Bool
    local v0 : Set[T]
    local v1 : T
    local v2 : <dict>
    local v3 : <dict>
fn std/set.len -> Int
  param v0 : Set[T] borrowed
  call direct std/hamt.count : Int
    local v0 : Set[T]
fn std/set.is_empty -> Bool
  param v0 : Set[T] borrowed
  binary eq : Bool
    call direct std/hamt.count : Int
      local v0 : Set[T]
    int 0
fn std/set.to_list -> List[T]
  param v0 : Set[T] borrowed
  call direct std/hamt.keys : List[K]
    local v0 : Set[T]
fn std/set.eq_go -> Bool
  param v0 : Set[T] owned
  param v1 : List[T] owned
  param v2 : Int owned
  param v3 : Int owned
  dict v4 : T owned
  dict v5 : T owned
  block : Bool
    loop L0
      block : Never
        let v6 : Bool
          if : Bool
            binary eq : Bool
              local v2 : Int
              local v3 : Int
            block : Bool
              bool true
            if : Bool
              call direct std/set.has : Bool
                local v0 : Set[T]
                intrinsic list_index : E
                  local v1 : List[T]
                  local v2 : Int
                local v4 : <dict>
                local v5 : <dict>
              block : Bool
                block : Never
                  let v7 : Set[T]
                    dup
                      local v0 : Set[T]
                  let v8 : List[T]
                    dup
                      local v1 : List[T]
                  let v9 : Int
                    binary add : Int
                      local v2 : Int
                      int 1
                  let v10 : Int
                    local v3 : Int
                  let v11 : Set[T]
                    local v7 : Set[T]
                  drop v0
                  assign v0
                    local v11 : Set[T]
                  let v12 : List[T]
                    local v8 : List[T]
                  drop v1
                  assign v1
                    local v12 : List[T]
                  assign v2
                    local v9 : Int
                  assign v3
                    local v10 : Int
                  continue L0
              block : Bool
                bool false
        drop v1
        drop v0
        return
          local v6 : Bool
    drop v0
    drop v1
fn std/set.hash_go -> Int
  param v0 : List[T] owned
  param v1 : Int owned
  param v2 : Int owned
  param v3 : Int owned
  dict v4 : T owned
  block : Int
    loop L0
      block : Never
        let v5 : Int
          if : Int
            binary eq : Bool
              local v1 : Int
              local v2 : Int
            block : Int
              local v3 : Int
            block : Int
              block : Never
                let v6 : List[T]
                  dup
                    local v0 : List[T]
                let v7 : Int
                  binary add : Int
                    local v1 : Int
                    int 1
                let v8 : Int
                  local v2 : Int
                let v9 : Int
                  binary add : Int
                    local v3 : Int
                    call method trait=2 hash slot=0 : Int
                      local v4 : <dict>
                      intrinsic list_index : E
                        local v0 : List[T]
                        local v1 : Int
                let v10 : List[T]
                  local v6 : List[T]
                drop v0
                assign v0
                  local v10 : List[T]
                assign v1
                  local v7 : Int
                assign v2
                  local v8 : Int
                assign v3
                  local v9 : Int
                continue L0
        drop v0
        return
          local v5 : Int
    drop v0
fn std/set.show_go -> List[String]
  param v0 : List[T] owned
  param v1 : Int owned
  param v2 : Int owned
  param v3 : List[String] owned
  dict v4 : T owned
  block : List[String]
    loop L0
      block : Never
        let v5 : List[String]
          if : List[String]
            binary eq : Bool
              local v1 : Int
              local v2 : Int
            block : List[String]
              local v3 : List[String]
            block : List[String]
              block : Never
                let v6 : List[T]
                  dup
                    local v0 : List[T]
                let v7 : Int
                  binary add : Int
                    local v1 : Int
                    int 1
                let v8 : Int
                  local v2 : Int
                let v9 : List[String]
                  block : List[String]
                    let v10 : List[String]
                      dup
                        local v3 : List[String]
                    let v11 : ?
                      box from=String
                        call method trait=3 show slot=0 : String
                          local v4 : <dict>
                          intrinsic list_index : E
                            local v0 : List[T]
                            local v1 : Int
                    let v12 : List[String]
                      intrinsic list_push : List[String]
                        local v10 : List[String]
                        local v11 : ?
                    drop v11
                    local v12 : List[String]
                let v13 : List[T]
                  local v6 : List[T]
                drop v0
                assign v0
                  local v13 : List[T]
                assign v1
                  local v7 : Int
                assign v2
                  local v8 : Int
                let v14 : List[String]
                  local v9 : List[String]
                drop v3
                assign v3
                  local v14 : List[String]
                continue L0
        drop v0
        return
          local v5 : List[String]
    drop v0
fn std/set.eq -> Bool impl_of=1/Set[T]
  param v0 : Set[T] owned
  param v1 : Set[T] owned
  dict v2 : T owned
  dict v3 : T owned
  block : Bool
    let v4 : Bool
      if : Bool
        binary ne : Bool
          call direct std/set.len : Int
            local v0 : Set[T]
          call direct std/set.len : Int
            local v1 : Set[T]
        block : Bool
          let v5 : Bool
            bool false
          drop v1
          local v5 : Bool
        block : Bool
          call direct std/set.eq_go : Bool
            local v1 : Set[T]
            call direct std/set.to_list : List[T]
              local v0 : Set[T]
            int 0
            call direct std/set.len : Int
              local v0 : Set[T]
            local v2 : <dict>
            local v3 : <dict>
    drop v0
    local v4 : Bool
fn std/set.hash -> Int impl_of=2/Set[T]
  param v0 : Set[T] owned
  dict v1 : T owned
  block : Int
    let v2 : Int
      call direct std/set.hash_go : Int
        call direct std/set.to_list : List[T]
          local v0 : Set[T]
        int 0
        call direct std/set.len : Int
          local v0 : Set[T]
        int 0
        local v1 : <dict>
    drop v0
    local v2 : Int
fn std/set.show -> String impl_of=3/Set[T]
  param v0 : Set[T] owned
  dict v1 : T owned
  block : String
    let v2 : List[String]
      listlit : List[String]
    let v3 : String
      block : String
        let v4 : String
          str "set.from(["
        let v5 : String
          block : String
            let v6 : String
              block : String
                let v7 : List[String]
                  call direct std/set.show_go : List[String]
                    call direct std/set.to_list : List[T]
                      local v0 : Set[T]
                    int 0
                    call direct std/set.len : Int
                      local v0 : Set[T]
                    local v2 : List[String]
                    local v1 : <dict>
                let v8 : String
                  str ", "
                let v9 : String
                  intrinsic join : String
                    local v7 : List[String]
                    local v8 : String
                drop v8
                drop v7
                local v9 : String
            let v10 : String
              str "])"
            let v11 : String
              binary concat : String
                local v6 : String
                local v10 : String
            drop v10
            drop v6
            local v11 : String
        let v12 : String
          binary concat : String
            local v4 : String
            local v5 : String
        drop v5
        drop v4
        local v12 : String
    drop v0
    local v3 : String
fn std/set.iter_start -> (List[T], Int) impl_of=4/Set[T]
  param v0 : Set[T] owned
  dict v1 : T owned
  dict v2 : T owned
  block : (List[T], Int)
    let v3 : (List[T], Int)
      tuple : (List[T], Int)
        call direct std/set.to_list : List[T]
          local v0 : Set[T]
        int 0
    drop v0
    local v3 : (List[T], Int)
fn std/set.iter_done -> Bool impl_of=4/Set[T]
  param v0 : Set[T] owned
  param v1 : (List[T], Int) owned
  dict v2 : T owned
  dict v3 : T owned
  block : Bool
    let v4 : (List[T], Int)
      local v1 : (List[T], Int)
    let v5 : List[T]
      dup
        tupleget 0 : List[T]
          local v4 : (List[T], Int)
    drop v5
    let v6 : Int
      tupleget 1 : Int
        local v4 : (List[T], Int)
    drop v4
    let v7 : Bool
      binary ge : Bool
        local v6 : Int
        call direct std/hamt.count : Int
          local v0 : Set[T]
    drop v0
    local v7 : Bool
fn std/set.iter_next -> (List[T], Int) impl_of=4/Set[T]
  param v0 : Set[T] owned
  param v1 : (List[T], Int) owned
  dict v2 : T owned
  dict v3 : T owned
  block : (List[T], Int)
    let v4 : (List[T], Int)
      local v1 : (List[T], Int)
    drop v0
    let v5 : List[T]
      dup
        tupleget 0 : List[T]
          local v4 : (List[T], Int)
    let v6 : Int
      tupleget 1 : Int
        local v4 : (List[T], Int)
    drop v4
    tuple : (List[T], Int)
      local v5 : List[T]
      binary add : Int
        local v6 : Int
        int 1
fn std/set.iter_get -> T impl_of=4/Set[T]
  param v0 : Set[T] owned
  param v1 : (List[T], Int) owned
  dict v2 : T owned
  dict v3 : T owned
  block : T
    let v4 : (List[T], Int)
      local v1 : (List[T], Int)
    drop v0
    let v5 : List[T]
      dup
        tupleget 0 : List[T]
          local v4 : (List[T], Int)
    let v6 : Int
      tupleget 1 : Int
        local v4 : (List[T], Int)
    drop v4
    let v7 : T
      intrinsic list_index : E
        local v5 : List[T]
        local v6 : Int
    drop v5
    local v7 : T
