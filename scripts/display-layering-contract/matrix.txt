# Every counted mutant runs the complete assertion set. The owner is a
# hand-written design fact: the one assertion this mutant and no other reddens.
# run.sh compares each mutant's observed red set against these rows byte for
# byte, so a mutant that reddens more (or less) than the rule it broke is a
# failure rather than a stronger result.

role	drop-display-question	counted
role	inherit-display	counted
owner	drop-display-question	display_wins_over_show
owner	inherit-display	display_is_not_inherited
red	drop-display-question	display_wins_over_show
red	inherit-display	display_is_not_inherited
control	show_stays_the_nested_rendering
