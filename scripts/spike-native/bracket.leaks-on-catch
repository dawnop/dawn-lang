This program takes fault and panic barriers on purpose, and drives failures
through `bracket` -- which is a taken barrier on the native side too, since
the release runs from a `longjmp` handler. A taken catch jumps past C frames
whose references nothing can release, so leak detection is off for it; see the
header of run.sh.
