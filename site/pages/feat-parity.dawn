use std/map
use std/str

# 同一份源码，两个后端逐字节同答案
pub fn main() -> Unit !io = {
  let m = map.from([(1, "a"), (2, "b")])
  println("${0.1 + 0.2}")
  println(str.to_upper("île"))
  println("${map.keys(m)}")
}
