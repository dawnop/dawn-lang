pub fn main() -> Unit !io = {
  range(1, 11)
    |> filter(fn(x) => x % 2 == 1)
    |> map(fn(x) => x * x)
    |> fold(0, fn(a, b) => a + b)
    |> fn(total) => println("sum of odd squares: $total")
}
