# A method call keeps its receiver: `1 |> xs.get()` is `xs.get(1)`, which is
# `get(xs, 1)`. The left side lands after the receiver, not before it, because
# a receiver is not an argument.
pub fn main() -> Unit !io = println("${1 |> [10, 20, 30].get()}")
