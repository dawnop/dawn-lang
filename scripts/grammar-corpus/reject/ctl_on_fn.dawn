# expect: `ctl` introduces `ctl effect Name { ... }`
# `ctl` modifies exactly one declaration form. Written in front of anything
# else it is the author reaching for a keyword, so the report names the form
# the word introduces rather than the generic list of what may start a
# declaration -- the same answer `opaque` gives, for the same reason.
ctl fn f() -> Int = 1
