D	default_params.dawn	809	810	effect operations do not take parameter defaults	take the argument explicitly at every call
D	default_params.dawn	765	766	trait methods do not take parameter defaults	wrap the trait method in a function that has one
D	default_params.dawn	931	932	impl methods do not take parameter defaults	calls dispatch through the trait's signature
D	default_params.dawn	369	372	a parameter default must be pure, but it calls `eff` (!io)	the default runs at every call that omits the argument; take the argument explicitly, or drop the effect
D	default_params.dawn	487	489	parameter `xs` cannot take a default value: its type List[T] mentions the function's type parameters	take the argument explicitly at the call sites
D	default_params.dawn	620	621	undefined variable: a	
D	default_params.dawn	709	712	parameter `a` is Int but its default value is String	
D	default_params.dawn	1114	1126	missing argument(s) for `mix`: c	signature: fn mix(a: Int, b: Int, c: Int) -> Int
