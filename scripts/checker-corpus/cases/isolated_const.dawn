# checker.dawn: the second caller of enter_isolated is the const initializer,
# and `enter_fn` does not reset Frame.isolated -- so a flag left set by a
# const would silence every later function in the module
effect Ask { fn ask() -> Int }

const N: Int = 1

pub fn raises_after_const() -> Int = ask()
