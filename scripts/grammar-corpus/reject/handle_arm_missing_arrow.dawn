# expect: `=>` (an arm is a closure)
# expect: allowed at module top level
# An arm is a closure written like a lambda; `=` is not the arrow that makes
# one, here or anywhere else.
effect Ask {
  fn ask() -> Int
}

fn f() -> Int = {
  with handle Ask { ask() = 1 }
  2
}
