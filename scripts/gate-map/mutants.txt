# Which assertion each mutant reddens, and which one of them owns it.
#
# Regenerate the `red` lines with `gatemap.py --record-mutants`. The `owner`
# and `control` lines are a hand edit, because which assertion owns a mutant is
# a design decision and a recorder that could reassign owners would launder the
# collision it exists to catch.
#
# Overlaps are recorded, not forbidden; what may not overlap is an owner. See
# the block comment above `Check` in gatemap.py for the five rules, and
# scripts/pipe-contract/matrix.txt for the precedent this copies.
#
# As recorded today every mutant reddens exactly one assertion, so there are no
# overlaps to read. That is a fact about this tree rather than a rule: the rule
# is only that an owner is not shared, and a later mutant is free to redden
# more than one thing as long as the one it owns stays its own.
#
# Two entries are not like the others:
#
#   unwatched_floor is a `control`. Nothing here owns it and nothing may redden
#   it. It is the sentence every mutant has to keep true, and it exists because
#   the failure it describes (rule B growing an appetite until the unwatched
#   set is empty and the map says everything is covered) happened twice while
#   this directory was being written.
#
#   glob-with-a-wildcard-head is `recorded` rather than counted, and its red
#   set is deliberately empty. With the guard in resolve() present the mutant
#   changes nothing, so it can own nothing; what makes it worth having is rule
#   3, which fails the moment it starts reddening anything. Its companion
#   glob-with-a-literal-head edits the same file and is counted, so if that
#   file is ever renamed the pair does not quietly go vacuous.

control	unwatched_floor

role	a-closed-gap-still-recorded	counted
owner	a-closed-gap-still-recorded	unseen_no_stale
red	a-closed-gap-still-recorded	unseen_no_stale

role	a-new-file-nobody-watches	counted
owner	a-new-file-nobody-watches	unseen_no_new
red	a-new-file-nobody-watches	unseen_no_new

role	a-recorded-gap-with-no-reason	counted
owner	a-recorded-gap-with-no-reason	unseen_reason_present
red	a-recorded-gap-with-no-reason	unseen_reason_present

role	a-recorded-gap-with-the-wrong-reason	counted
owner	a-recorded-gap-with-the-wrong-reason	unseen_reason_true
red	a-recorded-gap-with-the-wrong-reason	unseen_reason_true

role	corpus-target-that-is-not-here	counted
owner	corpus-target-that-is-not-here	corpus_target_exists
red	corpus-target-that-is-not-here	corpus_target_exists

role	drop-the-one-path-join	counted
owner	drop-the-one-path-join	python_join_probe
red	drop-the-one-path-join	python_join_probe

role	glob-with-a-literal-head	counted
owner	glob-with-a-literal-head	glob_literal_probe
red	glob-with-a-literal-head	glob_literal_probe

role	glob-with-a-wildcard-head	recorded

role	golden-forgets-a-module	counted
owner	golden-forgets-a-module	golden_covers_selfhost
red	golden-forgets-a-module	golden_covers_selfhost

role	golden-names-a-ghost-module	counted
owner	golden-names-a-ghost-module	golden_module_resolves
red	golden-names-a-ghost-module	golden_module_resolves

role	label-section-heads-a-missing-script	counted
owner	label-section-heads-a-missing-script	label_section_resolves
red	label-section-heads-a-missing-script	label_section_resolves

role	label-section-nobody-runs	counted
owner	label-section-nobody-runs	label_section_has_a_gate
red	label-section-nobody-runs	label_section_has_a_gate

role	prev-diff-drops-its-own-std	counted
owner	prev-diff-drops-its-own-std	prev_diff_std_premise
red	prev-diff-drops-its-own-std	prev_diff_std_premise

role	rewrite-a-shared-sentence	counted
owner	rewrite-a-shared-sentence	coupling_probe
red	rewrite-a-shared-sentence	coupling_probe

role	std-becomes-a-corpus-target	counted
owner	std-becomes-a-corpus-target	std_not_corpus
red	std-becomes-a-corpus-target	std_not_corpus

role	two-sections-one-differential	counted
owner	two-sections-one-differential	label_section_unique
red	two-sections-one-differential	label_section_unique

role	workflow-runs-a-missing-script	counted
owner	workflow-runs-a-missing-script	commands_resolve
red	workflow-runs-a-missing-script	commands_resolve
