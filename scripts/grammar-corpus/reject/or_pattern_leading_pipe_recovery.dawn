# expect: expected a pattern
# A pipe may continue a pattern only after a left alternative. Recovery must
# discard this broken arm and still find the following newline-separated arm.
fn recover(c: Choice) -> Int =
  match c {
    | Left(x) -> x
    Right(y) -> y
  }

fn after() -> Int = 3
