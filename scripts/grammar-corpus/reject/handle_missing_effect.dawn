# expect: the effect to handle
# expect: allowed at module top level
# `handle` is contextual: `with handle` that is not a binder must name the
# effect it answers, and an effect name is uppercase.
effect Ask {
  fn ask() -> Int
}

fn f() -> Int = {
  with handle { ask() => 1 }
  2
}
