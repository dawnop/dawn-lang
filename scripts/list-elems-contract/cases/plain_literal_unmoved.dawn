# The control: a list literal with neither element form. Nothing in this
# contract may move it.
pub fn main() -> Unit !io = {
  println(to_string([1, 2, 3]))
  println(to_string([0, if true { 1 } else { 2 }, 9]))
}
