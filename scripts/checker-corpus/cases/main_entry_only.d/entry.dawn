# Only the entry module's `main` is the entry point. The library next door
# defines a private `fn main(x: Int) -> Int` and nothing is said about it;
# this module's `main` has the wrong shape and no `pub`, and both are
# refused here, where the entry point is.
use lib.{helper}

fn main() -> Int = helper()
