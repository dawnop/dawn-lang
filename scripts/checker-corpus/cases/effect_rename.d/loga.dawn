# One of two libraries that export an effect called `Log` (SPC-06).
pub effect Log {
  fn log_a(line: String) -> Unit
}

pub type Level = Quiet | Loud

pub trait Shout[T] {
  fn shout(x: T) -> String
}
