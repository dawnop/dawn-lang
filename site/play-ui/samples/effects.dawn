# pure: no !io in the signature
fn double(n: Int) -> Int = n * 2

# touches IO, so the signature must say !io
fn shout(s: String) -> Unit !io = println("$s!")

pub fn main() -> Unit !io = {
  shout(to_string(double(21)))
}
