-- a variable absorbed by the io written beside it --
under_io ran
300
-- the row that says only !io, with the variable in a parameter --
300
-- the control: a row with no io to absorb anything --
300
-- two absorbed variables, two labels, one charge --
307
-- forwarded through a second absorbing signature --
7
-- a lambda whose own inferred row mixed io with a variable --
inside the lambda
300
-- and the same with two variables under the io --
inside the second lambda
307
-- and a pure closure through the same signatures --
1
