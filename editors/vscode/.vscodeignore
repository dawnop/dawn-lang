# What a published .vsix must not carry. `vsce ls --tree` prints what is left.
#
# The grammar's own test corpus is the largest item here and the least useful
# to an installed extension: it exists so CI can assert the scope rules, and it
# is the repository's business rather than the user's.
test/**
.vscodeignore
package-lock.json
*.vsix
