schema = 1
# The terminal vocabulary and its drivers. Was `tea` through 0.2.0; the split
# into tea_core + tea_term is the break, so the name moves with it.
name = "tea_term"
version = "0.3.0"

[deps]
tea_core = "../tea-core"
