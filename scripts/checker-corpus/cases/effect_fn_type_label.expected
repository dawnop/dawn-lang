D	effect_fn_type_label.dawn	1393	1409	a type declaration cannot name the effect `Ask`	write !io, or leave the function type pure: a declared effect belongs to the signature that raises it, and the handler travels with the closure
D	effect_fn_type_label.dawn	1431	1447	a type declaration cannot name the effect `Ask`	write !io, or leave the function type pure: a declared effect belongs to the signature that raises it, and the handler travels with the closure
D	effect_fn_type_label.dawn	312	328	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	effect_fn_type_label.dawn	382	398	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	effect_fn_type_label.dawn	584	600	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	effect_fn_type_label.dawn	612	628	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
D	effect_fn_type_label.dawn	467	483	a function type cannot name the effect `Ask`	use an effect variable: `fn() -> Int !e` accepts a closure that raises `Ask` and carries it on into this signature's own row. A written label would mean the caller supplies the handler, and in Dawn a closure carries the handler it was created under (docs/spec.md §6.5)
