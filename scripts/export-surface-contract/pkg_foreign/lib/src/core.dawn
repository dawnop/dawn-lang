# A package-private seam of `lib`, used by a sibling module of the same
# package and refused to the project that depends on `lib`.
pub(pkg) fn seam(n: Int) -> Int = n + 1

pub fn api(n: Int) -> Int = seam(n)
