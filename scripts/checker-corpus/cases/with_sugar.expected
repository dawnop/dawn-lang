D	with_sugar.dawn	753	755	`?` on an Option requires the function to return Option[...], but it returns Int	
D	with_sugar.dawn	287	295	`return` cannot cross the closure `with` introduced: the statements after `with` are that closure's body	drop the `return` — the block's last expression is already what the `with` call hands back
D	with_sugar.dawn	387	392	`break` cannot cross the closure `with` introduced: the statements after `with` are that closure's body	the loop is outside that closure; move the `with` inside the loop body, or call the resource-using part as its own function
D	with_sugar.dawn	353	396	the loop body's value is discarded (Int)	the last statement of a loop body must not be a non-Unit expression
D	with_sugar.dawn	465	473	`return` cannot cross a `with handle` arm: the arm is a closure the handler calls, not part of the enclosing function	drop the `return` — the arm's last expression is what the operation call hands back
D	with_sugar.dawn	570	571	cannot assign to `n` from inside a `with handle` arm	an arm is a closure and captures by value; hand the value back as the operation's result instead
D	with_sugar.dawn	596	597	the closure `with` introduced cannot capture `var` bindings (capture is by value)	bind it to a let before the `with`, or pass it as a parameter
D	with_sugar.dawn	683	684	a `with handle` arm cannot capture `var` bindings (capture is by value)	bind it to a let before the handler, or pass it in as an operation parameter
D	with_sugar.dawn	691	692	the closure `with` introduced cannot capture `var` bindings (capture is by value)	bind it to a let before the `with`, or pass it as a parameter
