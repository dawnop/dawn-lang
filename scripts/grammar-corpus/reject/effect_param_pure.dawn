# expect: `pure` is not an effect name
# The binder position, beside `effect_param_io.dawn`. A binder is where the
# name is chosen, so the refusal lands there rather than on the row this
# signature would then have written.
fn f[!pure](x: Int) -> Int = x
