type Secret = Secret
pub opaque type PublicBox[T] = Int

pub trait HasItem[T] {
  type Item
  fn item(x: T) -> T.Item
}

impl[T] HasItem[PublicBox[T]] {
  type Item = List[PublicBox[Secret]]
  fn item(x: PublicBox[T]) -> List[PublicBox[Secret]] = []
}
