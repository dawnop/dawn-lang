# The other library exporting `Log`. Its operation is named differently, so a
# consumer can bring both effects in by renaming, not only by qualifying.
pub effect Log {
  fn log_b(line: String) -> Unit
}

pub type Level = Low | High
