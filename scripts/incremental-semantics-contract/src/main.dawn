# The benchmark's entry. bench.py builds this project; the benchmark itself is
# `contract/bench` in the compiler package, because it drives analysis entry
# points whose signatures carry checker state, which is `pub(pkg)` there.
use compiler/contract/bench

pub fn main() -> Unit !io = bench.run()
