schema = 1
name = "selfhost"

# The self-hosted codegen drives the same ASM the Kotlin compiler uses (the
# version must stay in lock-step with compiler/build.gradle.kts).
[java-deps]
asm = "org.ow2.asm:asm:9.7.1"

[deps]
json = "../packages/json"
