# checker.dawn: a whole-module import whose alias a selective import already
# took. The reverse order is caught in bind_import, which reads the alias table
# on the way in; the whole-module branch had to learn to read the name table the
# same way, or the rule held in one source order only (#185).
use other.{lib}
use lib

pub fn f() -> Int = lib()
