# expect: this `if` condition parsed as a lambda
# expect: allowed at module top level
# The block after `=>` swallows the `{` that was meant to start the `if` body,
# so the failure surfaces far from its cause. Reported at the decision instead
# (spec §4.5).
fn f() -> Unit = {
  if x => { g() } { h() }
}
