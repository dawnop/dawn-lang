# expect: `with` is a statement and needs an enclosing block
# `with handle` claims the rest of *a block*; a body that is a bare expression
# has no rest to claim.
effect Ask {
  fn ask() -> Int
}

fn f() -> Int = with handle Ask { ask() => 1 }
