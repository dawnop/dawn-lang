# checker.dawn: `?` inside the closure `with` synthesizes, when that closure's
# result type is not yet known
fn poly[T](a: Int, k: fn(Int) -> T) -> T = k(a)

fn qmark(x: Option[Int]) = {
  with y <- poly(1)
  x?
}
