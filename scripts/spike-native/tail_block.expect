-- the block joins the arguments already there --
[2, 4, 6]
-- a parameter head over two parameters --
6
-- a bare name applies to the closure alone --
3
-- a dot call grows the argument --
[2, 3]
-- effects ride through the block --
x=1
x=2
x=3
-- named argument, default, and the block fill their own slots --
gap=12 align=1
body
gap=0 align=1
body2
-- the block's value is the closure's result --
[T]
