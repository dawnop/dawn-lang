# expect: an effect operation cannot declare effects of its own
# An operation's effect is the effect that declares it (docs/effects-design.md
# 3.1); what an arm's body does is charged where the handler is installed, so
# there is nothing an annotation here could mean.
effect Ask {
  fn ask() -> Int !io
}
