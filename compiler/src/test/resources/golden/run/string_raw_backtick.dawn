# Raw strings: backticks, no escapes, no interpolation, newlines kept verbatim.
use std/str

pub fn main() -> Unit !io = {
  println(`no $interp, no \n escapes, "quotes" ok`)
  println(`multi
line raw`)
  let empty = ``
  println(to_string(str.len(empty)))
  println(`<span class="s">$x</span>` ++ " tail")
}
