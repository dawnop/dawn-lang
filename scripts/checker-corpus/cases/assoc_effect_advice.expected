D	assoc_effect_advice.dawn	1465	1468	`run` is declared !io but trait `Job` declares it pure	drop !io here, or declare the trait method !io; if impls of `Job` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!M.E`
D	assoc_effect_advice.dawn	1564	1571	function `stray` is not declared !io but calls `println` (!io)	add !io to the end of the signature, or remove the call
D	assoc_effect_advice.dawn	1412	1419	function `run` is not declared !io but calls `println` (!io)	add !io to the end of the signature, or remove the call; if impls of `Job` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!M.E`
D	assoc_effect_advice.dawn	1762	1768	function `step` is not declared !io but calls `helper` (!io)	add !io to the end of the signature, or remove the call; if impls of `Task` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!M.E`
