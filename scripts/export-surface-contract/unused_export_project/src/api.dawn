# Nothing imports `never_imported`, and the exporter still has to refuse it: a
# public contract is wrong where it is written, not where it is used
# (design §7.1).
type Secret = | Secret

pub fn never_imported() -> Secret = Secret

pub fn clean() -> Int = 0
