# A module-qualified call: the alias is not a receiver, so the left side is the
# first argument and nothing is inserted in front of it.
use std/str

pub fn main() -> Unit !io = println("${"dawn" |> str.starts_with("da")}")
