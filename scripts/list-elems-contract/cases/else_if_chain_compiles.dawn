# An `else if` chain with no final else is one element form as a whole, so its
# bodies need not be Unit.
fn f(n: Int) -> List[Int] = [0, if n == 1 { 11 } else if n == 2 { 22 }, 9]

pub fn main() -> Unit = ()
