# expect: impl methods cannot declare their own effect parameters
# The trait method an impl method answers has no parameter table at all
# (grammar.ebnf trait_method), so a binder here would bind something the
# declaration it is checked against cannot name. The row introduces it instead.
impl Show[Int] {
  fn show[!e](x: Int) -> String = "x"
}
