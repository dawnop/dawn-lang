# checker.dawn: leaving a comptime block clears the isolation flag
# (Frame.isolated) -- an unhandled effect after the block is still reported,
# because the refusal that belongs to `leave_isolated` no longer applies
effect Ask { fn ask() -> Int }

pub fn raises() -> Int = {
  let n = comptime { 2 }
  ask()
}
