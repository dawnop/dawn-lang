# expect: string patterns cannot contain interpolation
# A pattern must be a constant; an interpolated string is not one. Rejected in
# the parser (spec 5), not left for the checker.
fn f(s: String) -> Int =
  match s {
    "a$s" -> 1
    _ -> 0
  }
