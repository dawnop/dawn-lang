# EBNF use_decl / module_use / import_list, plus `use a/b as c` -- the form the
# EBNF is missing entirely (SYN-04's first item).
use std/str
use std/map as m
use json/value.{Json, JStr}
# whole-module and selective imports of one path, the pair §10.2 allows
use json/lexer
use json/lexer.{Tok}
use java "java.lang.Math"

fn f() -> Int = 1
