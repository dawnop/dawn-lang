D	effect_type_args.dawn	984	993	`Pair` binds no effect parameters	an effect argument answers a `[!e]` in the declaration's parameter list, and this one has none
D	effect_type_args.dawn	1085	1094	`List` binds no effect parameters	an effect argument answers a `[!e]` in the declaration's parameter list, and this one has none
D	effect_type_args.dawn	1310	1319	`Cell` takes 0 type parameter(s), got 1	`Cell` also binds the effect parameter(s) `!e`, written with the `!`: `Cell[!io]`
D	effect_type_args.dawn	1408	1423	`Thunk` binds 1 effect parameter(s), got 2	
D	effect_type_args.dawn	1598	1607	`Cell` is a record type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Cell` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	effect_type_args.dawn	1693	1703	`Chain` is a variant type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Chain` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	effect_type_args.dawn	1919	1930	`Hidden` is an opaque type and takes no effect argument	a named type is its name and its type arguments, so there is nowhere for the row to be remembered. Write `Hidden` and let the consumer's row cover it, or declare a transparent `alias`, which expands
D	effect_type_args.dawn	2148	2153	unknown effect `Nope`	declare it with `effect Nope { ... }` (or import it); effect variables are lowercase
D	effect_type_args.dawn	2379	2380	function `io_slot` is not declared !io but calls `t` (!io)	add !io to the end of the signature, or remove the call
D	effect_type_args.dawn	2496	2497	`label_slot` uses the effect `Ask`, but its signature does not declare !Ask	add !Ask to the signature, or answer it here with `with handle Ask { ... }`
D	effect_type_args.dawn	2661	2662	function `var_slot` is not declared !e but calls `t` (!e)	add !e to the end of the signature, or remove the call
D	effect_type_args.dawn	2967	2968	function `own_var` is not declared !e but calls `m` (!e)	`!e` is `Mapper`'s own effect parameter, so writing `!e` on the end of the signature would bind a different variable: write `Mapper`'s effect argument at the type application, spelling a row this signature does bind, or declare !io, which covers any row
