# the declaring module: an alias and an opaque type that bind an effect
# parameter, exported so the qualified path resolves them
pub alias Thunk[!e] = fn() -> Int !e

pub alias Mapper[T, U, !e] = fn(T) -> U !e

pub type Cell[!e] = { f: fn() -> Int !e }
