# expect: comparison operators cannot be chained
# spec 4.3: comparisons do not associate. `a < b < c` is a syntax error rather
# than the (b<c)-compared-to-a nonsense C inherits.
fn f(a: Int, b: Int, c: Int) -> Bool = a < b < c
