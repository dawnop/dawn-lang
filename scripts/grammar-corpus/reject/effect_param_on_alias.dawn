# expect: alias declarations cannot declare effect parameters
# The `fn` table only, for now: the alias and record forms are knife 3 of
# docs/effect-params-design.md, and until they land a binder here would bind a
# variable that `refuse_decl_effvar` refuses two lines later anyway.
alias Pair[T, U, !e] = (T, U)
