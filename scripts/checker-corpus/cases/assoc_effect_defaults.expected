D	assoc_effect_defaults.dawn	1804	1805	an associated effect binds a ground row, and `!e` is not one	bind a declared effect, `!io`, or the empty row `!()`; an effect variable or a projection cannot be bound
D	assoc_effect_defaults.dawn	1372	1375	`run` is declared !io but trait `P` declares it pure	drop !io here, or declare the trait method !io; if impls of `P` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!T.E`
D	assoc_effect_defaults.dawn	1525	1526	impl `Q[A]` is missing `effect E`	bind it: `effect E = !<effect>` (or `!io`, or the empty row `!()`)
D	assoc_effect_defaults.dawn	1847	1848	impl `R[A]` is missing `effect E`	bind it: `effect E = !<effect>` (or `!io`, or the empty row `!()`)
D	assoc_effect_defaults.dawn	2179	2182	`hop` must write the effect(s) `Ask`: the trait's row (after this impl's bindings) carries them	each written effect is a hidden evidence parameter, so the labels are part of the method's shape; declare the method !Ask
D	assoc_effect_defaults.dawn	1215	1222	function `run` is not declared !io but calls `println` (!io)	add !io to the end of the signature, or remove the call; if impls of `P` need different rows, declare `effect E` in the trait, bind it in each impl (`effect E = !io`, or `effect E = !()` where the impl is pure), and write the method's row as `!T.E`
