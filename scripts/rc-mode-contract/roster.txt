# The sites the mode-contract mutants run at: `owner:name:arity:index prog`.
# Chosen from the borrowed knife's own target profile (docs/perceus-design.md
# 6): std functions with a heap-typed parameter, hot in scan-shaped code, no
# reassignment of the parameter in the body. run.sh ratchets this list both
# ways against the corpus files next to it.
#
# std/pvec:index is deliberately not like the others: `xs[i]` lowers to the
# Core intrinsic `list_index`, and only the C emitter maps that onto
# std/pvec.index -- so the function's executed call sites are OUTSIDE the
# mode table's jurisdiction, and the coherent flip leaks (the callers keep
# the all-owned intrinsic convention while the callee stops dropping). It
# stays on the roster as the probe that pins that boundary; see README.
std/cursor:done:2:0   cursor_scan
std/cursor:next:2:0   cursor_scan
std/str:len:1:0       str_len
std/pvec:index:2:0    pvec_index
std/list:reverse:1:0  list_ops
std/list:take:2:0     list_ops
