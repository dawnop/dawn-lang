-- a lambda whose row unifies against label-beside-variable --
307
-- a named value whose written row has to be assignable to it --
1011
-- two labels left over, so the tail is a join --
49
-- nothing left over: the control that was always green --
24
-- the same row forwarded through a second signature --
89
-- and carried into bracket's use closure --
78
-- and into sort_by's comparator --
[3, 2, 1]
-- widen taken as a value rather than called --
112
