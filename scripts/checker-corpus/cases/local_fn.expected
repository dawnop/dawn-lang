D	local_fn.dawn	77	82	local functions cannot declare effect variables	a local function is `!io` or pure; lift it to the top level for effect polymorphism
D	local_fn.dawn	177	180	the body of `inner` has type String but the declared return type is Int	
D	local_fn.dawn	221	226	the body of `inner` performs io	declare it: fn inner(...) -> Unit !io = ...
