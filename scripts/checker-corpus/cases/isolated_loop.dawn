# checker.dawn: leaving a comptime block puts the enclosing loop stack back
# (Frame.loop_stack) -- the first `break` is still inside the loop, the second
# one (the anchor) really is outside
pub fn breaks() -> Unit = {
  for i in [1, 2] {
    let n = comptime { 2 }
    break
  }
  break
}
