fn shape(xs: List[Int]) -> String =
  match xs {
    [] -> "empty"
    [..init, 9] -> "ends in nine, init len ${len(init)}"
    [..init, last] -> "last $last after ${len(init)}"
  }

pub fn main() -> Unit !io = {
  println(shape([]))
  println(shape([1, 2, 9]))
  println(shape([1, 2, 3]))
}
