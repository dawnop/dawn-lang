use std/str
use std/io

pub fn main() -> Unit !io = {
  match io.read_file("/definitely/not/here.txt") {
    Ok(_) -> println("ok?!")
    Err(e) -> println("failed: ${str.contains(e, "here.txt")}")
  }
}
