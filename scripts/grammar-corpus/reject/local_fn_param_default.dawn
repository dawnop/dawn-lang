# expect: local function parameters cannot take default values
# A local function is a function value to its callers -- no signature to
# carry a default -- so the parse refuses it where a top-level fn accepts it.
fn outer() -> Int = {
  fn inner(a: Int = 1) -> Int = a
  inner(2)
}
