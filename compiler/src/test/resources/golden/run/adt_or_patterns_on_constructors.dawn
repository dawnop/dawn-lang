type Color = | Red | Green | Blue

fn warm(c: Color) -> Bool =
  match c {
    Red | Green -> true
    Blue -> false
  }

pub fn main() -> Unit !io = {
  println("${warm(Red)} ${warm(Green)} ${warm(Blue)}")
}
