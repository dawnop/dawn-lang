schema = 1
# The vocabulary-agnostic half of the Elm architecture: the `Tree` contract,
# the reconciler written against it, the pre-order walk, `trait App`, and
# subscriptions. Nothing here names a widget, a style or a terminal, and
# nothing here has an effect row -- that is the whole reason it is a package
# of its own rather than a subdirectory of tea-term (see packages/tea-core/README.md).
name = "tea_core"
version = "0.1.0"
