D	eq_hash.dawn	191	193	functions cannot be compared	compare the values they produce, not the functions
D	eq_hash.dawn	387	390	`Float` has no hash	NaN and -0.0 give Float two different equalities, so no hash can agree with `==`; use an Int encoding (scaled integer, bits) or a String rendering instead
D	eq_hash.dawn	468	469	functions cannot be compared, and `Boxed` contains one	compare the values they produce, not the functions
D	eq_hash.dawn	468	469	functions cannot be hashed, and `Boxed` contains one	hash the values they produce, not the functions
