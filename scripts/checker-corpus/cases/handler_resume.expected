D	handler_resume.dawn	2414	2422	arm `plain` binds a continuation, but effect `Plain` is not declared `ctl`	write `ctl effect Plain { ... }`: suspendability is declared on the effect, because a row names effects and that is all the Java boundary can read
D	handler_resume.dawn	2670	2671	arm `ask` returns Int but it answers the block, which is String	a control arm's value is the value of the whole `with handle`, not the operation's result -- that is what the `resume` clause bought
D	handler_resume.dawn	3041	3044	arm `ask` takes 0 parameter(s) and a resume binder, but operation `ask` declares 1	the binder is the continuation, not a parameter: fn(Int) -> Int
D	handler_resume.dawn	3059	3060	`k` is not a function (it is Int)	
D	handler_resume.dawn	3203	3207	argument type mismatch: expected Int, got String	type: fn(Int) -> Int
D	handler_resume.dawn	3441	3449	`?` on an Option requires the block this arm answers to be Option[...], but it is Int	
D	handler_resume.dawn	3664	3669	`break` cannot cross a `with handle` arm: the arm is a closure the handler calls, not part of the enclosing function	the loop is outside the arm; a control arm runs where the handler was installed, not where the operation is raised, and it may suspend there
