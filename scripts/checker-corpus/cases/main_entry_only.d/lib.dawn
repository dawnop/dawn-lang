# A library module's `main` is an ordinary function (spec §10.5, SPC-04):
# private, returning an Int, taking an argument -- none of it is refused.
fn main(x: Int) -> Int = x + 1

pub fn helper() -> Int = main(41)
